# Mockup v2 — "Áo mới" (đã chốt 2026-07-13)

Nguồn-sự-thật **thị giác** cho diện mạo macOS v2. Trước đây các bản này chỉ sống trong
scratchpad phiên brainstorm (ephemeral) nên bị mất dấu khi reconcile — nay lưu vào repo.
Quy chiếu decision-log entry **2026-07-13 "Diện mạo mới v2"** (9 quyết định) khi đọc.

| File | Là gì | Dùng cho |
|------|-------|----------|
| `mockup-v2-tabbed.html` | **Popover 3 tab** (Hôm nay · Chuông · Bộ gõ) — spec chính xác từng px/màu/chữ | Bước 1 (áo popover), Bước 3–5 |
| `ao-moi-mindful-key.html` | Bản tổng quan "áo mới" (plan chốt) | Toàn cảnh |
| `soi-lai-mockup.html` | Vòng **Soi lại** 4 nhịp (Nhận ra → Cho phép → Soi → Nuôi dưỡng) | Bước 4 |
| `board_hi.png` | Whiteboard brainstorm — có sơ đồ **cửa sổ 6 mục nav** (gộp 4-tab/macro/convert/about) | Bước 2 |

**Xem nhanh:** mở HTML bằng trình duyệt, hoặc render:
`"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless --screenshot=out.png --window-size=420,820 mockup-v2-tabbed.html`
Trong `mockup-v2-tabbed.html`, đổi `set("today")` → `set("bell")`/`set("input")` để render từng tab.

**Ràng buộc HIẾN CHƯƠNG khi bám mockup:** dòng sông = **1 hue teal, chỉ đổi biên độ** — KHÔNG
màu tốt/xấu, KHÔNG số/streak/heatmap. Chưa có dữ liệu → hiện trạng thái trống thật thà, **không
vẽ phẳng lặng/số giả** (§2.2 + decision-log dec.4).
