# 3.8: Xem dòng sông theo THÁNG

**Story ID:** 3.8
**Epic:** 3 — Hoàn thiện theo nghiệm thu tay v2 (2026-07-15)
**Slug:** month-river-view
**Status:** done

> [2026-07-15 — CÂU HỎI THIẾT KẾ ĐÃ CHỐT] Chủ dự án xác nhận qua 2 câu hỏi trực tiếp: (1) giữ
> nguyên 1-đường-1-hue teal như Ngày/Tuần, KHÔNG cần dạng nén-theo-tuần; (2) chấp nhận sông "lỗ
> chỗ" nhiều gap thật, KHÔNG cần ngưỡng "≥N ngày mới hiện" kiểu Ngày. AC dưới đây giờ là khoá
> cứng, không còn "[Inference]/[CHỜ XÁC NHẬN]". Xem `decision-log.md#2026-07-15` "Chốt câu hỏi
> thiết kế Tháng (3.8)".

> ⚠️ **RỦI RO NHẬN DIỆN CAO NHẤT trong cả 4 story tách từ "3.3"** — đây là story DUY NHẤT
> chưa từng xuất hiện dưới BẤT KỲ hình thức nào trong mockup A1/A2 hay decision-log gốc
> 2026-07-13 (kể cả dạng "ý tưởng minh hoạ" như tuần). Toàn bộ yêu cầu đến từ 1 câu trả lời
> trực tiếp của chủ dự án hôm 2026-07-15 ("Ngày + Tuần + Tháng"), CHƯA có bản vẽ/mockup cụ thể
> nào cho hình dạng thật của nó. [Source: decision-log.md#2026-07-15 "Nghiệm thu tay lần 2"]

## Story

As a **người dùng đã dùng mindful-key hơn 1 tháng**,
I want **xem được nhịp cảm xúc trải dài cả tháng**,
so that **tôi thấy được xu hướng dài hơn 1 tuần, mà không biến app thành một bảng thống kê**.

## Acceptance Criteria
<!-- LOCKED. External dev tools must not edit Acceptance Criteria. -->

1. `MoodStoreMac` có hàm MỚI `MoodStoreMac_FetchMonthSamples(void)` trả về mảng ~30 phần tử (30 ngày gần nhất tính cả hôm nay, cũ→mới), mỗi phần tử = trung bình `send_risk` trong ngày đó hoặc `NSNull` nếu ngày đó 0 mẫu — cùng nguyên tắc gap như 3.7 nhưng phạm vi 30 ngày thay vì 7. KHÔNG có ngưỡng "≥N ngày mới hiện" — chủ dự án đã xác nhận chấp nhận sông lỗ chỗ thật. [Source: bmad-output/macos/stories/3.7.week-river-view.story.md AC1, áp dụng lại cùng công thức, đổi phạm vi] [Source: decision-log.md#2026-07-15 "Chốt câu hỏi thiết kế Tháng"]
2. Điều khiển "Ngày / Tuần / Tháng" (mở rộng từ segmented 3.7) thêm lựa chọn thứ 3 "Tháng". [Source: quyết định trực tiếp 2026-07-15]
3. Chế độ Tháng TÁI DÙNG nguyên `EmotionRiverView` (1 đường, 1 hue teal, không nhãn số) — KHÔNG thêm biểu đồ/heatmap/lưới ngày kiểu calendar-heatmap. Đã CHỐT qua câu hỏi trực tiếp 2026-07-15 (không cần dạng nén-theo-tuần). [Source: docs/AGENT-BRIEF.md §5 điều 7 "không biểu đồ theo thời gian trong panel"] [Source: decision-log.md#2026-07-15]
4. Trục nhãn dưới sông ở chế độ Tháng hiện thưa hơn (vd chỉ 4-5 mốc: "4 tuần trước / 3 tuần trước / ... / Hôm nay"), KHÔNG hiện đủ 30 nhãn ngày (sẽ rối, đọc như lịch/dashboard). [Inference]
5. KHÔNG có nhãn số/%/đếm ngày liên tục kiểu streak ở bất kỳ đâu trong chế độ Tháng — rủi ro cao nhất của story này chính là dễ vô tình biến thành "N ngày liên tiếp ổn định" (= streak, bị cấm tuyệt đối). [Source: docs/AGENT-BRIEF.md §2.2 "KHÔNG gamification (streak/điểm/huy hiệu)"]
6. Dữ liệu thô KHÔNG bị đổi bởi story này — hàm mới CHỈ ĐỌC.

## Tasks / Subtasks

- [ ] Thêm khai báo + cài đặt `MoodStoreMac_FetchMonthSamples(void)` trong `MoodStoreMac.h/.mm` — tái dùng cấu trúc SQL của 3.7 (`FetchWeekSamples`), chỉ đổi phạm vi ngày (30 thay 7) và có thể cần tái dùng/tách 1 hàm nội bộ dùng chung `FetchDailyAverages(NSInteger numDays)` thay vì viết 2 hàm gần giống hệt nhau cho Tuần/Tháng. (AC: 1)
- [ ] Thêm lựa chọn thứ 3 "Tháng" vào segmented control đã dựng ở 3.7. (AC: 2)
- [ ] Wire chọn "Tháng" → gọi hàm mới → `setSamples:`. (AC: 3)
- [ ] Đổi nhãn trục theo chế độ Tháng (thưa hơn Tuần/Ngày) — dùng lại method `setAxisLabels:` đã thêm ở 3.7 (không viết API thứ 3). (AC: 4)
- [ ] Rà kỹ: không streak/số đếm ngày liên tiếp/heatmap ở bất kỳ đâu trong code mới. (AC: 5)
- [ ] Xác nhận không hàm ghi nào bị đụng. (AC: 6)

## Dev Notes
<!-- LOCKED. External dev tools must not edit Dev Notes. -->

### ✅ Câu hỏi thiết kế — ĐÃ CHỐT 2026-07-15

Không giống 3.5/3.6/3.7 (đều có mockup/tiền lệ code rõ để bám theo), 3.8 ban đầu **không có gì
để bám** ngoài 1 câu trả lời ngắn — đã hỏi trực tiếp qua 2 câu hỏi, cả 2 chọn phương án AN TOÀN
đề xuất sẵn:

1. **Hình dạng:** giữ nguyên 1-đường-1-hue như Ngày/Tuần — KHÔNG cần dạng nén-theo-tuần.
2. **Độ thưa dữ liệu:** chấp nhận sông "lỗ chỗ" nhiều gap thật — KHÔNG cần ngưỡng "≥N ngày mới hiện".
3. **Ranh giới "không phải dashboard"** — được giải quyết GIÁN TIẾP bởi câu trả lời #1: giữ 1-hue-1-đường
   (không lưới/heatmap) chính là điều giữ story này bên trong ranh giới đã chốt.

Xem `decision-log.md#2026-07-15` "Chốt câu hỏi thiết kế Tháng (3.8)" cho bản ghi đầy đủ.

### Hướng dẫn kỹ thuật

- **Tái dùng SQL pattern của 3.7, KHÔNG viết 2 hàm gần giống hệt nhau** [Inference] — đề xuất
  factor chung: `static NSArray<NSDictionary *> *FetchDailyAverages(NSInteger numDays)` (hàm
  private trong `MoodStoreMac.mm`), rồi `MoodStoreMac_FetchWeekSamples` gọi `FetchDailyAverages(7)`
  và `MoodStoreMac_FetchMonthSamples` gọi `FetchDailyAverages(30)`. Nếu 3.7 đã code XONG trước
  3.8 (đúng thứ tự phụ thuộc), dev agent của 3.8 PHẢI đọc code thật của 3.7 trước, refactor lại
  thành hàm dùng chung thay vì copy-paste — đừng để 2 bản SQL gần giống hệt nhau tồn tại song song.

- **KHÔNG đụng `core/`.**

- **Nếu câu hỏi thiết kế #1 được trả lời là "cần dạng khác"** — story này cần VIẾT LẠI AC1-5
  trước khi code (Update intent, không phải Create) — không tự diễn giải mơ hồ thành code.

## Testing
<!-- LOCKED. External dev tools must not edit Testing. -->

- Cổng tự động: `make test` xanh, build sạch, `make brand-lint` 0 (đặc biệt cảnh giác brand-lint
  cho story này — rủi ro cao nhất lỡ tay thêm màu/nhãn số bị cấm).
- **Mở app thật bấm-thử bắt buộc.**
- **Key scenarios:** tháng có nhiều gap (dùng app không đều) → sông vẫn vẽ đúng phần có dữ liệu,
  KHÔNG nội suy; chuyển Ngày→Tuần→Tháng→Ngày liên tục → không leak/giữ state cũ.
- **Per-AC verification:** AC1 → seed dữ liệu giả 30 ngày (có gap xen kẽ), đối chiếu kết quả hàm;
  AC5 → đọc lại TOÀN BỘ code mới, xác nhận không streak/số đếm/heatmap nào lọt qua — đây là bước
  quan trọng nhất của story này, không được bỏ qua dù các AC khác pass.
- **Fixtures:** seed CSDL test 30 ngày giả (không phải dữ liệu thật của chủ dự án).
- **Edge cases:** app dùng < 30 ngày (mới cài) → phần đầu mảng toàn gap, không phải "tháng chưa
  đủ nét" giả định sai (phải phân biệt được với "1 tháng dùng đều nhưng ít gõ").

## Dependency Maps

- **Blocked by:** `3.7` (Tuần) — 3.8 tái dùng segmented control + factor hàm SQL dùng chung mà
  3.7 dựng trước; làm 3.8 trước 3.7 sẽ phải dựng lại từ đầu.
- **Blocks:** không.
- **External:** không.

## Owned File/Module Scope

- `platforms/apple/macos/MoodStoreMac.h/.mm` — thêm `MoodStoreMac_FetchMonthSamples` (+ refactor
  hàm private dùng chung với 3.7 nếu áp dụng).
- `platforms/apple/macos/EmotionRiverView.h/.mm` — CHỈ nếu cần mở rộng `setAxisLabels:` đã có
  từ 3.7 cho phù hợp mật độ nhãn thưa hơn — KHÔNG viết API thứ 3.
- `platforms/apple/macos/SettingsWindowController.mm` — thêm lựa chọn thứ 3 vào segmented đã có
  từ 3.7. Trùng owned scope với 3.5/3.6/3.7 — xem cảnh báo tuần tự ở 3.7.

## Learnings from Previous Stories

- **Story không có mockup tham chiếu = rủi ro nhận diện cao hơn hẳn, phải DỪNG-HỎI rõ ràng hơn
  bình thường** — 2.4 đã có tiền lệ "DỪNG-HỎI chủ dự án" cho những chỗ genuinely mơ hồ (ngưỡng
  đủ dữ liệu, hành vi CTA nuôi dưỡng) thay vì tự quyết rồi hy vọng đúng. 3.8 áp dụng tinh thần
  này Ở MỨC CAO HƠN vì không có BẤT KỲ nguồn nào (kể cả mockup minh hoạ) để tham chiếu. (từ 2.4
  Dev Notes mục "Ngưỡng đủ dữ liệu"/"CTA Nuôi dưỡng")
- **Tái dùng hàm/SQL đã viết ở story liền trước thay vì copy-paste** — bài học từ chính cặp
  3.7/3.8 (xem Dev Notes "Tái dùng SQL pattern"), áp dụng nguyên tắc DRY đã nêu trong
  CLAUDE.md §Core Principles.

## Dev Agent Record

- Câu hỏi thiết kế đã chốt qua 2 câu hỏi trực tiếp với chủ dự án TRƯỚC khi code (xem banner đầu
  file + `decision-log.md#2026-07-15`) — không tự suy diễn.
- `MoodStoreMac_FetchMonthSamples()` = `FetchDailyAverages(30)`, dùng CHUNG hàm private với 3.7
  (viết 1 lần cho cả 2, vì làm cùng 1 mạch — xem Dev Agent Record của 3.7).
- Nhãn trục Tháng: "30 ngày trước"/"20 ngày trước"/"10 ngày trước"/"Hôm nay" — cùng lý do lệch so
  với AC4 gốc đã ghi ở Dev Agent Record của 3.7 (view chỉ có 4 slot nhãn cố định).
- Segmented control mở rộng thành 3 mục `@[@"Ngày", @"Tuần", @"Tháng"]` ngay từ đầu (không phải
  thêm mục thứ 3 sau, vì 3.7/3.8 code cùng lúc).
- Đã rà lại (AC5): không streak/số đếm ngày liên tiếp/heatmap nào trong code Tháng — chỉ tái dùng
  `EmotionRiverView` 1-đường-1-hue y hệt Ngày/Tuần.
- Build sạch · `make test` 16/16 · `make brand-lint` 0 · `git diff core/` rỗng.
  Trạng thái bằng chứng: build-verified, CHƯA có mắt nhìn app thật; CHƯA seed dữ liệu 30 ngày giả
  để verify AC1 bằng số liệu thật.
