# platforms/linux

`linux-upstream/` là bản copy nguyên trạng của `OpenKey/Sources/OpenKey/linux/` — hiện chỉ có
1 file `README.md` ở thượng nguồn, **chưa có code vỏ Linux thật** (IBus/Fcitx chưa viết).

Xem cảnh báo năng lực theo Desktop Environment ở `../README.md` trước khi cam kết tính năng
gác cổng gửi tin hoạt động giống macOS.
