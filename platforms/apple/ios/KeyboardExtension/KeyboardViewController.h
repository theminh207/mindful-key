//
//  KeyboardViewController.h
//  mindful-key — iOS keyboard extension (Round 1 walking skeleton)
//
//  Mốc A: bàn phím tự vẽ CHỈ hiện danh sách phím — chạm phím chèn thẳng ký tự thô (không qua
//  core/engine). Mốc B mới nối vKeyHandleEvent() để gõ Telex đúng.

#import <UIKit/UIKit.h>

@interface KeyboardViewController : UIInputViewController

@end
