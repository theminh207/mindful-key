# TEST_MATRIX — bảng hành vi → bằng chứng

> Đây là **sổ cái bằng chứng**, không phải danh sách tính năng. Mỗi dòng là một
> hành vi sản phẩm; các cột kiểm thử cho biết hành vi đó **đã được chứng minh tới
> đâu**. Cột **Bằng chứng** phải trỏ tới thứ có thật (file test, lệnh chạy, output) —
> không ghi suông. Nếu một hành vi ghi `implemented` mà Bằng chứng = `none`, đó là
> tín hiệu *"code có thể build sạch nhưng chưa ai chứng minh nó chạy thật"* (đúng
> kiểu bug bước 4: `MoodWatchMac_Init()` chưa từng được gọi). Không tô màu, không
> chấm điểm — chỉ mô tả trạng thái.

## Cách đọc các cột

- **Engine (unit):** chứng minh ở tầng bộ não C++ thuần (`tests/core/test_engine` qua `make test-core`). Dùng chung mọi OS.
- **macOS (platform):** chứng minh ở vỏ macOS thật (`platforms/apple/macos`, build/chạy/quan sát hành vi).
- **iOS (platform):** chứng minh ở vỏ iOS thật (`platforms/apple/ios`, keyboard extension) — xem khối "Đội iOS" cuối bảng. **Build-verify** (`xcodebuild -sdk iphonesimulator` BUILD SUCCEEDED, tự động) ≠ **đã có người gõ/nhìn** trên simulator/máy thật — 2 mức bằng chứng khác nhau, ghi rõ trong ô Bằng chứng, không gộp lại thành ✅ chung chung.
- **E2E:** chứng minh luồng người dùng đầu-cuối (vd macOS: gõ trong Zalo → gác cổng hiện → nuốt Enter → gửi lại).
- **Bằng chứng:** trỏ cụ thể. `none` = chưa có gì.
- **Trạng thái:** `planned` · `in_progress` · `implemented` · `changed` · `retired`.

Ký hiệu ô: `✅` có bằng chứng · `⚠️` một phần/gián tiếp · `❌` cần mà chưa có · `—` không áp dụng.

## Bảng

| Hành vi | Engine | macOS | E2E | Bằng chứng | Trạng thái |
|---|---|---|---|---|---|
| Gõ Telex → Unicode, bỏ dấu cơ bản | ✅ | — | — | `tests/core/test_engine` 5 case (`make test-core`) | implemented *(chỉ unit)* |
| Ghép vần / luật bỏ dấu nâng cao (`Vietnamese.cpp`) | ⚠️ | — | — | gián tiếp qua 5 case, chưa có case riêng | planned |
| Tự đổi Việt/Anh theo app (`SmartSwitchKey`) | ❌ | ❌ | — | none | planned |
| MoodBuffer gom từ → câu | ❌ | — | — | none | planned |
| **Chấm điểm send-risk 0–1 (`core/mood/SendRiskAnalyzer`) — MỘT bản dùng chung macOS · iOS · Windows** | ✅ | ✅ | — | **[2026-07-16]** `make test-core` → `tests/core/test_send_risk` 27 ca xanh (công thức bão hoà · ép sàn chửi nặng · hạ chữ thường tiếng Việt · dấu câu tách từ · bóp ký tự lặp · danh mục). Số kỳ vọng KHÔNG tự nghĩ: lấy từ lần chạy đối chiếu thật biên dịch bản core CẠNH bản iOS cũ trong 1 binary — **25/27 trùng tới 1e-12**, 2 ca lệch là 2 ca dấu câu (bản iOS cũ sai, core theo macOS). Bảng hạ chữ thường tiếng Việt viết tay: quét **89/89 chữ hoa, 0 lệch** so với `NSString lowercaseString` của Apple. macOS: `make build` BUILD SUCCEEDED, 0 warning mới (warning `NSStringFromWString` đã tồn tại ở HEAD trước khi sửa — verify bằng `git show HEAD`). iOS: `make test-ios` xanh, số kỳ vọng do đội iOS tự tính vẫn khớp không cần sửa | implemented |
| Vỏ Windows gọi `SendRiskAnalyzer` của core (thay bản dán nhãn cảm xúc cũ) | ✅ | — | ❌ | none — chưa port (Mốc 2 module 1 đang dở) | planned |
| MoodWatch đọc cảm xúc chạy thật trên máy | — | ⚠️ | — | bước 4 vá `Init` chưa gọi; chưa có test chứng minh chạy | in_progress |
| **Gác cổng Enter-không-Shift (Zalo/Discord)** | — | ❌ | ❌ | none — luồng chính chưa có bằng chứng E2E | in_progress |
| **[GĐ2] Gác cổng gửi tin — vỏ Windows (Feature #1)** | — | — | ❌ | **[2026-07-17]** `60d0702` — CI Windows XANH (Debug+Release). Bắt `VK_RETURN` không Shift trong hook có sẵn (`OpenKey.cpp:512`), gọi `core/mood/BreathingPause` (không viết lại logic quyết định), nuốt Enter rồi hiện hộp 2 nút. Chống vòng lặp dùng `dwExtraInfo` — cơ chế OpenKey ĐÃ có sẵn (`OpenKey.cpp:499`), đúng vai `myEventSource` bên macOS. Danh sách app **RỖNG mặc định** (máy dev là macOS, không xác minh được tên tiến trình Windows → không bịa); người dùng tự thêm app đang mở qua menu khay, lưu registry. **CHƯA ai nhìn hộp thoại thật** — ca nghiệm thu `docs/QA-WINDOWS.md` §4b (G0–G11), test được ngay trong Notepad không cần app chat | implemented *(build-verified, chưa ai nhìn)* |
| MoodStore mã hóa (AES-256 + Keychain, at-rest) | — | ✅ | — | **[2026-07-16]** `make test-macos` (tests/macos/mood_pipeline) Ca 6: đọc 15 byte đầu `mood.enc` sau khi ghi mẫu thật ≠ header `SQLite format 3` — file at-rest đúng là ciphertext, không phải SQLite trần | implemented |
| Consent gate hỏi 1 lần lúc khởi động | — | ❌ | — | none | implemented (?) |
| Chuông/Nudge — cooldown 45s, snooze 1 giờ | — | ❌ | — | none | implemented (?) |
| Reflection cuối ngày (`FetchTodaySummary`) | — | ❌ | — | none | implemented (?) |
| Xuất brand asset (SVG → PNG/.icns) | — | ✅ | — | `make brand` / `brand/export.sh` chạy được | implemented |
| **Ràng buộc nhận diện phủ được vỏ Windows** (GĐ0.1) | — | — | ✅ | **[2026-07-17]** Trước đó CẢ BA lớp gác (`brand_lint.py` · `brand-guard.sh` · `.githooks/pre-commit`) đều lọc theo đuôi file và đều thiếu `.cpp` → 15 file vỏ Windows chưa từng bị quét, dù CLAUDE.md mô tả đây là ràng buộc CỨNG "chặn mọi tool/agent". Verify bằng cách **đâm vào từng lớp** (nhét `streak` vào `MoodWatch.cpp`): `make brand-lint` exit≠0 · `brand-guard.sh` exit 2 (chặn agent) · pre-commit exit 1. Quét 133→155 file. **CÒN HỞ:** `core.hooksPath` chưa set trên máy chủ dự án → lớp pre-commit không chạy (`make hooks` để bật) | implemented |
| **Bảng màu 3 vỏ sinh từ `brand/tokens.json`, không chép tay** (GĐ0.3) | — | ✅ | ✅ | **[2026-07-17]** `brand/gen-palette.py` sinh 9 colorset + `BrandPalette.h` (apple) + `BrandPalette.h` (windows, mới). Trung thành: tái tạo 9 colorset **y hệt từng byte** (git diff rỗng). Đối chiếu bằng máy với `git HEAD`: **0 màu đổi**, 0 mất, +1 `CardWhite`; header Windows khớp Apple từng giá trị. Khoá chống trôi: `make brand-palette-check` + bước CI — thử **cả 2 chiều** (đổi tokens quên sinh lại / sửa tay file sinh ra) đều exit 1. `make test` xanh · `make build` BUILD SUCCEEDED | implemented |
| Đóng gói DMG ad-hoc | — | ✅ | — | verify mount + `codesign -dv` sạch (`docs/INSTALL.md`) | implemented |
| **[2026-07-10] Thay áo 4 cửa sổ legacy macOS (mindful-key) theo NOW BRAND OS** | | | | | |
| Cửa sổ Điều khiển (4 tab): checkbox cam → PillSwitch teal, tab chọn hết phụ thuộc Accent Color hệ thống | — | ⚠️ | ❌ | build sạch (0 warning mới, đối chiếu baseline qua git stash) + `ibtool` (4→2 notice, không phát sinh mới) + khối card Gác cổng (story 1.4, đang dở) verify nguyên vẹn **2 lần độc lập**; CHƯA ai nhìn app thật. `mindful-key` commit `3b41b3d` | implemented |
| Cửa sổ Gõ tắt (Macro): checkbox/nút → PillSwitch/CTAButton/SecondaryButton | — | ⚠️ | ❌ | build sạch (11 warning = baseline) + `make test` xanh + `ibtool` cải thiện (43→42); CHƯA ai nhìn app thật. `mindful-key` commit `d370161` | implemented |
| Cửa sổ Chuyển mã (Convert): checkbox/nút → PillSwitch/CTAButton/SecondaryButton | — | ✅ | ❌ | **Duy nhất trong đợt có test hành vi thật** — lldb gọi trực tiếp IBAction xác nhận: toggle↔UserDefaults, 4 tùy chọn HOA/thường loại trừ lẫn nhau, 1 lần chuyển mã thật ("Xin chào các bạn"→"xin chào các bạn"), nút Đóng, giữ trạng thái qua relaunch app. Vẫn KHÔNG phải người nhìn — là lldb gọi hàm hộ. `mindful-key` commit `7c759a7` (+ vá notice `6b652af`) | implemented |
| Cửa sổ Thông tin (About): logo sóng `~` thay "V" đỏ, vá bug đè chữ "Trang GitHub"/credit, checkbox/nút → PillSwitch/CTAButton | — | ⚠️ | ❌ | build sạch (warning diff rỗng so baseline) + credit "Mai Vũ Tuyên (GPL v3)" verify byte-identical qua `grep` + logo verify qua `iconutil` export ngược; CHƯA ai nhìn app thật xem layout đè chữ đã hết hẳn chưa. `mindful-key` commit `2a4a090` | implemented |
| Radio "Chế độ gõ" (Việt/English) tô teal đầy đủ (không chỉ chữ label) | — | ❌ | — | AppKit không có API tint công khai cho glyph radio (cùng lý do PillSwitch phải tự vẽ) — cố ý CHƯA làm, vì thay control rủi ro gãy cơ chế loại-trừ Việt/Anh mà sandbox không click-test được | planned |
| **[2026-07-11] Đội iOS — Round 1 walking skeleton (8 story, `bmad-output/ios/stories/`)** | | | | | |
| Target iOS build từ `project.yml` chung (FR-A03, story 1.5) | — | ✅¹ | ⚠️ | `xcodegen generate` exit 0 (3 scheme) + `xcodebuild -sdk iphonesimulator` BUILD SUCCEEDED cho `MindfulKeyiOS`+`MindfulKeyKeyboard`, `make build` macOS vẫn xanh — build-verify thật, đóng Risk R5 tech-spec. Commit `dfb083d`. Chưa ai chạy app trên simulator | implemented |
| Gõ Telex ra dấu qua `core/engine` — Mốc B (FR-A01, story 1.2) | ✅¹ | ✅¹ | ❌ | `KeyboardBridge_HandleKeyTap` route qua `vKeyHandleEvent`; `tests/ios/bridge_test.mm` 5 ca Telex→Unicode PASS qua bridge (`make test-ios`, commit `91a8742`); build-verify BUILD SUCCEEDED. **CHƯA có ai gõ "vieetj"→"việt" trong Notes/Zalo thật** — đây là hành vi lõi, E2E vẫn ❌ | implemented *(bridge-verified, chưa device)* |
| Shift/Caps + lớp số & ký hiệu (FR-A02, story 1.3) | — | ✅¹ | ❌ | 3-state Shift (off/one-shot/lock), double-tap, 123↔ABC, hit ≥44pt — code + build-verify (commit `c6c042d`). Hành vi chạm/double-tap CHƯA ai thử trên simulator | implemented *(build-verified only)* |
| Loại ô mật khẩu — không đọc/log/mạng (FR-A07, story 1.4) | — | ✅ | ❌ | audit tĩnh (grep) xác nhận 0 `NSLog`/App-Group-nội-dung/network trong 2 file phạm vi + build sạch (commit `7760f38`) — đây là NEGATIVE test (chứng minh KHÔNG có hành vi xấu), mạnh hơn build-verify thường. Chưa ai gõ thật vào field `secureTextEntry=YES` | implemented |
| App Group heartbeat — container phát hiện "đã kích hoạt" (FR-A06, story 1.6) | — | ✅¹ | ❌ | entitlements 2 target + `AppGroupBridge` (chỉ timestamp/bool) — code + build-verify cả 3 scheme (iOS×2 + macOS), 1 bug link C++ name-mangling sửa tận gốc (commit `6e83b8d`). Round-trip heartbeat thật (viết ở extension, đọc ở container trên máy) CHƯA thử | implemented *(build-verified only)* |
| Onboarding 2 màn (kích hoạt + Full Access) + Home (FR-A04/A05, story 1.7) | — | ⚠️² | ❌ | dựng theo `EXPERIENCE.md`, build-verify BUILD SUCCEEDED + 1 lần cài+mở trên Simulator không crash (Màn 01 render đúng wireframe) — gần nhất với "có người nhìn" trong đợt này, nhưng chưa đi hết luồng (chưa bấm thật qua Cài đặt hệ thống, chưa VoiceOver/Reduce-Motion). Asset nhận diện (glyph/wordmark/copy) là **placeholder**, đánh dấu `TODO(Q10b)` — chưa chốt | implemented *(1 lần smoke, chưa full walkthrough)* |
| `tests/ios` test thật — bridge + build-smoke (FR-A17, story 1.8) | ✅ | ✅ | — | `make test-ios` chạy CẢ HAI: bridge test (5 ca) + `build_smoke.sh` (`xcodebuild` simulator); verify RED thật (bẻ scheme → exit 2, trả lại → 0) — không còn no-op. Commit `0c1ade7` | implemented |
| ⌫ backspace / sửa giữa từ ở engine (Loại 6, story 1.1) | ✅ | — | — | `tests/core/test_engine.cpp` +3 ca, kỳ vọng lấy từ `make test-core` thật (không đoán); phát hiện 1 quirk engine (`as⌫⌫nawm`→"nawm" không "năm" — biến hình không tái kích hoạt sau ⌫-về-rỗng qua âm tiết có dấu), đã khóa làm mốc regression, chưa báo đội core. Commit `6ff72f0` | implemented |
| Đo RAM extension trong giới hạn jetsam ~48–60MB | — | ❌ | ❌ | none — cần Instruments trên simulator/thiết bị thật, không tự động hóa được | planned |
| ~~Gác cổng gửi tin xuyên app trên iOS (Feature #1 bản macOS)~~ | — | — | — | **bất khả thi** — keyboard extension sandbox không có global hook/không thấy nút Gửi host app (FRICTION-LOG 2026-07-10, đã chốt) | retired |
| Con sóng `~` cảm xúc + tiếng chuông (Round 2, FR-A08/A09/A10) | — | — | — | Q1–Q3 đã chốt (decision-log 2026-07-13) + R2 story 2.1–2.6 ĐÃ code xong (commit tới `c872c0c`). **Các dòng bằng chứng R2 chi tiết CHƯA backfill vào bảng này** (nợ sổ — doc tụt sau code) | implemented *(R2 shipped, rows pending backfill)* |
| **[2026-07-13] Đội iOS — Round 3 (nhật ký + soi lại + theme)** | | | | | |
| Kho nhật ký cảm xúc mã hóa on-device + consent (FR-A13 nền, story 3.1) | — | ✅ (host) | ❌ | `tests/ios/mood_journal_store_test` 6 ca host PASS qua `make test-ios`: AES-256 round-trip, **negative test "không plaintext trên đĩa"** (`rangeOfData` không thấy 16B event dựng tay + giải mã lại đúng), cổng consent (chưa consent → KHÔNG tạo file), `DeleteAll`, `SetConsent(NO)` xóa sạch; **verify RED thật** (bẻ 1 assert → fail → sửa lại); build-smoke extension iOS Simulator BUILD SUCCEEDED; `git diff core/` rỗng. Kho ở `platforms/apple/shared/MoodJournalStore.mm`, wire ghi ở `MoodBridge.mm` SAU cổng ô-bảo-mật. **Device-only CHƯA verify:** (1) chia sẻ khóa Keychain cross-process extension↔container qua keychain-access-groups (cần Team ID thật, ký ad-hoc không verify được — nếu `errSecMissingEntitlement` là chỗ soát đầu tiên); (2) round-trip 2 tiến trình thật (extension ghi/container đọc cùng file App Group); (3) RAM extension với kho + Keychain + crypto nạp | implemented *(host-verified crypto/consent; cross-process + RAM device-only chưa)* |
| **[2026-07-14] Đội macOS — Diện mạo mới v2 (Epic 2)** | | | | | |
| Kho nhật ký lấy mẫu (Story 2.3) | — | ✅ | ❌ | build sạch (0 warning mới) + `make test` xanh + `make brand-lint` 0 lỗi. Tự động tích lũy sum/count qua `analyzeRecentTextAsync` và gọi `MoodStoreMac_LogSampleEvent`. `git diff core/` rỗng. CHƯA ai nhìn app thật. Commit `b35fb2b` | implemented |
| UI Check-in 1 chạm (Story 2.3) | — | ✅ | ❌ | Cấu hình heartbeat timer nội bộ trên Main Queue trong `PanelViewController` hiện `NSPanel` sau thời gian `vBellInterval`, lưu 3 mức sóng `MoodStoreMac_LogCheckinEvent`. Commit `b35fb2b` | implemented |
| Đổi độ nhạy 3 mức (Story 2.3) | ✅ | ✅ | ❌ | `make test` test C++ (`NudgeCoordinatorIOS` tương đương) xanh. macOS đọc trực tiếp biến cài đặt (thay vì hằng số cứng). Tự test "bật/tắt" qua test shell. CHƯA gõ thật xem E2E có đúng 5/3/2 câu không. Commit `b35fb2b` | implemented |
| Cửa sổ quản lý 6 mục nav trái (Story 2.2) | — | ⚠️ | ❌ | ~~✅~~ **HẠ CẤP 2026-07-15 khi có mắt nhìn thật:** build sạch, `make test` xanh, `make brand-lint` 0 lỗi, instantiate VC + chuyển tab chạy (commit `66cce8b`) — nhưng nghiệm thu tay phát hiện **mục "Hệ thống" trắng trơn hoàn toàn** (F1) và **4/6 mục lệch thiết kế** (F2/F5/F6/F7). Khung nav 6 mục đúng mockup; NỘI DUNG bên trong thì chưa. Xem `bmad-output/macos/ACCEPTANCE-v2-2026-07-15.md` | implemented *(khung đúng, nội dung chưa)* |
| Emotion River (Story 2.4) | — | ✅ | ❌ | build sạch, `make test` xanh. `MKRiverCanvas` render gap (`NSNull`). `SettingsWindowController` nhúng river đúng tab Hôm nay. Commit `fa76313` | implemented |
| Cửa sổ Soi lại (Reflection Screen) (Story 2.4) | — | ⚠️ | ❌ | ~~✅~~ **HẠ CẤP 2026-07-15 khi đọc kỹ code (audit "cái gì chưa nối dây"):** build sạch, `make test` xanh, `ReflectionScreenMac_Show` chia 4 nhịp, nút gọi callback cập nhật giờ (commit `fa76313`) — nhưng nội dung THẬT là NSTextField phẳng (comment tự thừa nhận "không có library, ta vẽ text mô tả"), KHÔNG vẽ sông, KHÔNG trục Sáng/Trưa/Chiều/Tối, số thô không che ("Đỉnh gợn hôm nay: 0.73"), và có 1 dòng hardcode placeholder `"(đã tính theo code)"` lộ thẳng ra người dùng thật (đã vá riêng dòng này, xem dòng epic-4/1.3 bên dưới — nhưng phần vẽ sông thật vẫn CÒN, xem `epic3_acceptance` gate 3 trong sprint-status.yaml) | implemented *(chạy được, chưa khớp mockup)* |
| Tab Chuông - Nhịp định kỳ (Story 2.5) | — | ✅ | ❌ | build sạch, `make test` xanh. Thêm thẻ "Nhịp" có MKSegmented (15, 30, 60), chọn theo khoảng gần nhất, gọi `BellMac_ApplySettings()` khi đổi. Không rớt `kBellSoundMuteName`. Commit `6d15a0b` | implemented |
| Pane Riêng tư - Xuất CSV & Tự xóa (Story 2.6) | — | ✅ | ❌ | build sạch, `make test` xanh. Thêm `PrivacyPaneView`, bật tắt consent, xuất CSV (thu hẹp), hẹn tự xóa (mặc định 90 ngày) gọi `DELETE`. Commit `c5646e9` | implemented |
| Khóa toàn bộ UI macOS ở chế độ Sáng (Option A, Follow-up) | — | ✅ | ❌ | Bổ sung `NSApp.appearance = NSAppearanceNameAqua` tại `AppDelegate.m`. Khắc phục triệt để lỗi "khung đen, lõi trắng" khi macOS ở Dark Mode, giúp đồng nhất 100% với bản vẽ HTML tĩnh. Build sạch, test xanh. CHƯA mở app thật để ngắm. | implemented |
| Sửa lỗi cắt cúp UI Popover (Bug fix) | — | ✅ | ❌ | Bỏ thao tác gán `self.view.frame` thủ công trong `PanelViewController` khi đổi chiều cao. Khắc phục triệt để lỗi popover bị khoảng trống ở trên hoặc bị xén mất header/tab/phần "Nhịp" ở các màn hình có chiều cao giới hạn do xung đột với layout auto-resize của `NSPopover`. Build sạch, test xanh. Đã chạy thử nghiệm. | implemented |
| Hoàn thiện tính năng lõi Chuông & Nhận diện (Story 2.7) | — | ✅ | ❌ | 1. Thêm 2 file âm thanh `Chuông chùa.wav` và `Chuông gió.wav` sinh tự động để chuông kêu thật thay vì `Glass` fallback. 2. Sửa thuật toán làm sạch chữ ở `MoodWatchMac.mm` để loại bỏ dấu câu (chấm phẩy, phẩy, hỏi, than...) trước khi phân tích, giúp bắt trúng từ khoá ngay cả khi dính liền với dấu câu. Build sạch, test xanh. | implemented |
| Di chuyển "Nhận diện độ nhạy" sang tab "Hôm nay" (Cân đối giao diện) | — | ✅ | ❌ | SensCard tách thành `SensitivityCardView`, nhúng dưới Gatekeeper trong Hôm nay. Popover co giãn mượt mà. | implemented |
| Khắc phục lỗi reo chuông và Quyền thông báo (Bug fix) | — | ✅ | ❌ | Thêm yêu cầu quyền thông báo `UNUserNotificationCenter` lúc start. Sửa lỗi thay đổi giờ yên lặng ở UI không cập nhật bộ nhớ `vBellFrom/vBellTo` trong `controlTextDidEndEditing:`. | implemented |
| Đưa khung cuộn NSScrollView vào Popover (Đồng nhất UI) | — | ✅ | ❌ | Gom nội dung các tab động vào `NSScrollView` cục bộ của `PanelViewController`. Cố định Header/Tab bar và Footer không bị cuộn mất khi Popover quá dài. | implemented |
| Cấu hình phím tắt nhanh cho Chuông & Bộ gõ (Hotkeys) | — | ✅ | ❌ | Bổ sung nút ghi âm phím nóng trực tiếp (phím tắt chuông mặc định `⌥⌘B`, bộ gõ `⌥Z`) bên cạnh switch ở tab Chuông và Bộ gõ. Tích hợp event intercept trong `OpenKey.mm` và đồng bộ qua NotificationCenter. | implemented |
| **[2026-07-15] NGHIỆM THU TAY LẦN 1 — chủ dự án mở app thật, xem 5/6 mục nav cửa sổ quản lý (có ảnh)** | | | | | |
| **Mục "Hệ thống" hiện được nội dung** (F1) | — | ❌ | ❌ | **PHẢN CHỨNG bằng mắt:** pane trắng trơn hoàn toàn — không tiêu đề, không control. Code: `SettingsWindowController.mm:388` show `_openKeyVC.tabviewSystem`; outlet CÓ thật (`Main.storyboard:1871`). **Nguyên nhân chưa biết** — giả thuyết `@property(weak)` (`ViewController.h:21`) + `removeFromSuperview` → box giải phóng → nil; nhưng CHƯA giải thích được vì sao `tabviewPrimary` y hệt cơ chế lại hiện tốt. Cấm vá mò | in_progress *(Epic 3 gate 4)* |
| **Pane "Hôm nay" có card Gác cổng (Feature #1) nổi nhất** (F2) | — | ❌ | ❌ | **PHẢN CHỨNG bằng mắt:** không có card gác cổng, không link "Soi lại →", không chân trang riêng tư — chỉ tiêu đề + khung sông rỗng. Code xác nhận đây là **việc chưa làm**, không phải bug: `mk_buildTodayPane` (`SettingsWindowController.mm:333-337`) chỉ dựng title + `EmotionRiverView`. Chạm HIẾN CHƯƠNG §5 điều 10 (Feature #1 luôn nổi nhất) | in_progress *(Epic 3 gate 3)* |
| **Lối vào màn "Soi lại" 4 nhịp từ cửa sổ** (F3) | — | ❌ | ❌ | **PHẢN CHỨNG bằng mắt:** không có lối vào. `ReflectionScreenMac_Show` tồn tại + build sạch (`fa76313`) nhưng **không ai gọi được từ cửa sổ** → dòng "Cửa sổ Soi lại (Story 2.4)" phía trên là code-có-thật-nhưng-người-dùng-không-tới-được | in_progress *(Epic 3 gate 3)* |
| **Mọi mục xem hết được nội dung (không cắt đáy)** (F5) | — | ⚠️ | ❌ | **Phát hiện bằng mắt:** "Chuông" cụt ở thanh Âm lượng, "Riêng tư" cụt giữa nút "Xóa toàn bộ nhật ký". Nguyên nhân truy được ngay, **2 lớp**: `kMaxPaneH = 472.0` cố định (`SettingsWindowController.mm:55`) + cả file không có `NSScrollView`; VÀ pane tự khai chỉ cao 472 nên nội dung dài hơn bị đẩy xuống dưới y=0 (bọc scroll thôi KHÔNG cứu). **ĐÃ VÁ cả 2 lớp** (Epic 3 G1): scroll + pane cao theo `preferredHeight` thật. Bằng chứng mới: build sạch (1 warning linker = baseline) + `make test` xanh + brand-lint 0 + `git diff core/` rỗng. **CHƯA ai nhìn app thật** → giữ `⚠️`, không lên `✅` | in_progress *(G1 chờ mắt nhìn)* |
| **Nhãn control hiện đủ chữ** (F6) | — | ⚠️ | — | **Phát hiện bằng mắt** + **đo số thật** (`NSString.size(withAttributes:)`, systemFont 13pt): "Tạm tắt Mindful Keyboard bằng phím ⌘" cần **237.6pt** / có **191pt** → cụt 47pt. Đổi tên "Mindful Key" vẫn cần 202.4pt → vẫn cụt. Nới box thì đụng cửa sổ legacy còn sống (`AppDelegate.m:192/196/244`) → bỏ. **ĐÃ VÁ:** copy → "Tạm tắt bộ gõ bằng phím ⌘" (166pt, vừa). Đo lại 5 nhãn còn lại: **đều vừa** — kể cả "Tạm tắt chính tả bằng phím ⌃" (179.2/188) mà mắt tưởng sát mép → **ảnh đánh lừa, số thì không**. CHƯA ai nhìn app thật | in_progress *(G1 chờ mắt nhìn)* |
| **Cam CHỈ dùng cho CTA + link active** (HIẾN CHƯƠNG §5 điều 6) (F7) | — | ⚠️ | — | **NGHI PHẠM, thấy bằng mắt:** chấm CAM đánh dấu "Bộ tiếng" đang chọn ở tab Chuông — "đang chọn" là TRẠNG THÁI, không phải CTA. `make brand-lint` 0 lỗi mà vẫn lọt → lint chưa bắt được ca này. Cần soát: chấm chọn hay một phần glyph? | in_progress *(Epic 3 gate 2)* |
| **1 token teal duy nhất cho mọi PillSwitch ON** (F8) | — | ⚠️ | — | **thấy bằng mắt:** PillSwitch ở Bộ gõ nhạt hơn hẳn ở Chuông, cùng trạng thái ON → 2 sắc teal khác nhau. Nguyên nhân chưa truy | in_progress *(Epic 3 gate 1)* |
| Khung nav 6 mục (đúng thứ tự, chấm trung tính, mục chọn nền tealLight) | — | ✅ | — | **Mắt nhìn thật xác nhận ĐÚNG mockup A2** — đây là dòng ✅-thật đầu tiên của Epic 2 (không phải build-verify). Đủ 6 mục, đúng thứ tự Hôm nay·Chuông·Bộ gõ·Riêng tư·Hệ thống·Giới thiệu; Bộ gõ có sub-nav 3 mục | implemented |
| Không có vi phạm hiến chương ở 5 màn đã xem (ngoài F7) | — | ✅ | — | **Mắt nhìn thật:** không đèn đỏ/xanh cảm xúc, không mặt cười, không streak/điểm/huy hiệu, không copy khiển trách. Copy "Riêng tư" đúng giọng quan sát ("Quên có chủ đích — các sự kiện cũ sẽ tự biến mất…") | implemented |
| Mục "Giới thiệu" (credit Mai Vũ Tuyên + GPL v3) | — | ❌ | — | none — **nghiệm thu 2026-07-15 CHƯA xem mục này**. Hệ trọng pháp lý (GPL v3 + credit). Đừng suy diễn "không nhắc = đã đạt" | implemented (?) |
| **[2026-07-15] Chặng 1/2 (Epic 3+4) — 4 fix đầu tiên sau nghiệm thu, chủ dự án ra lệnh "triển khai bám plan"** | | | | | |
| Check-in 3 sóng chạy độc lập việc mở popover (F12, epic-4/1.2) | — | ⚠️ | ❌ | **Xác nhận bằng đọc code (không cần chạy thử để biết chắc):** timer trước đây dựng trong `PanelViewController -loadView`, không có gì ép `.view` nạp trước khi bấm icon 〜 → user không đụng icon = check-in không bao giờ tồn tại trong phiên đó. ĐÃ VÁ: chuyển sang `-init`, chạy độc lập việc mở popover. build sạch · make test xanh · brand-lint 0 · `git diff core/` rỗng. CHƯA ai chạy app cả ngày không đụng icon để xác nhận check-in giờ nổ ra thật | implemented *(build-verified, chưa device)* |
| Gác cổng: dòng thống kê "N lần dừng, M lần đợi" trong Soi lại (F14, epic-4/1.3) | — | ⚠️ | ❌ | `MoodStoreMac_FetchTodaySummary()` trước đây **0 nơi gọi** dù header comment từ đầu ghi dùng nó — code chết đúng kiểu tiền lệ `MoodWatchMac_Init()`. QUYẾT: nối, không xoá. ĐÃ VÁ + vá kèm placeholder `"(đã tính theo code)"` lộ ra người dùng (xem dòng "Cửa sổ Soi lại" phía trên). build sạch · make test xanh · brand-lint 0 · `git diff core/` rỗng. CHƯA ai bấm "Soi lại hôm nay..." để xác nhận 2 dòng mới hiện đúng | implemented *(build-verified, chưa device)* |
| Chấm "Bộ tiếng" đang chọn dùng teal, không cam (F7, Epic 3 gate 2) | — | ⚠️ | — | **Xác nhận vi phạm thật:** `_bellIndicator` di chuyển theo `_bellSelectedIndex` — đúng nghĩa chấm-đánh-dấu-đang-chọn, không phải glyph tĩnh. ĐÃ VÁ: `[Brand orange]` → `[Brand teal]`, khớp `MKSegmented` cùng file. Soát thêm 3 chỗ dùng cam khác trong `platforms/apple/macos/` — cả 3 đều ĐÚNG luật (link active/CTA), không sửa. build sạch · make test xanh · brand-lint 0 | implemented *(build-verified, chưa device)* |
| Chỉ 1 UI cấu hình Chuông trong toàn app (F13, Epic 3 gate 5) | — | ⚠️ | — | Phát hiện lúc audit: menu tray "Cài đặt Chuông tỉnh thức..." mở `BellMac_ShowSettings()` — NSAlert đời cũ, ghi CHUNG UserDefaults key với `BellSettingsView` mới nhưng KHÔNG chặn sàn 15 phút vừa chốt (lách thẳng qua quyết định riêng tư F10 cùng ngày). ĐÃ VÁ: xoá hẳn hàm cũ + helper riêng của nó, menu giờ mở thẳng mục "Chuông" của cửa sổ Cài đặt. build sạch · make test xanh · brand-lint 0 | implemented *(build-verified, chưa device)* |
| **[2026-07-15] Nghiệm thu tay lần 2 — chủ dự án tự chạy app, gửi 3 ảnh + 3 vấn đề** | | | | | |
| Sampling cảm xúc chạy độc lập popover, có consent thật (kiểm bằng `defaults read` máy chủ dự án) | ✅ | — | ✅ | **Không phải bug:** `g_sampleTimer` (MoodWatchMac.mm:286) tạo trong `MoodWatchMac_Init()` — gọi từ `applicationDidFinishLaunching`, độc lập hoàn toàn popover (khác lớp bug F12 của check-in). `defaults read vn.gnh.mindfulkey` xác nhận `MoodStoreConsentGranted=1`, `vBellInterval=15`. Sông rỗng lúc chụp ảnh là ĐÚNG hành vi — mỗi 15 phút chỉ ghi 1 điểm, cần ≥3 điểm (~30-45 phút gõ thật trải dài) mới vẽ được, đúng quyết định 2026-07-13 | implemented |
| 11 công tắc tab "Bộ gõ ▸ Kiểu gõ" đều có nối dây thật (không nút chết) | ✅ | ✅ | — | Grep 11 biến UserDefaults tương ứng: cả 11 đều được `OpenKey.mm`/engine đọc lại ở ≥3 chỗ ngoài file UI. Phát hiện 1 dây phụ thuộc: "Kiểm tra chính tả" TẮT khoá 3 nút khác (`ViewController.m:253-258`, đúng thiết kế, không phải bug) — dễ đọc nhầm thành "nút hỏng" nếu không biết | implemented |
| Bố cục 11 công tắc "Bộ gõ ▸ Kiểu gõ" cân đối, không lệch (F15) | — | ⚠️ | — | **PHẢN CHỨNG bằng mắt (chủ dự án tự thấy) + xác nhận bằng toạ độ:** 6 hàng cột trái (y=196→24, cách đều 35pt) vs 5 hàng cột phải (y=196→55) — dòng "Tắt tiếng Việt khi bộ gõ hệ thống khác tiếng Anh" trơ trọi ở đáy, không cặp bên phải. ĐÃ VÁ: nới nhãn `MKLblOtherLang` 300pt→436pt, đọc thành 1 hàng full-width riêng thay vì 1 ô cột trái bị rớt lại. build sạch (0 ibtool notice mới) · make test xanh · brand-lint 0 · `git diff core/` rỗng. CHƯA ai nhìn app thật | implemented *(build-verified, chưa device)* |
| Màn "Hôm nay" xem được sông theo Ngày/Tuần/Tháng (Epic 3 gate 3, phình scope) | — | ❌ | ❌ | none — **0 hàm** `FetchWeekSamples`/`FetchMonthSamples` tồn tại (đã grep xác nhận). Chủ dự án chốt 2026-07-15 mở rộng phạm vi gate 3 thêm Tuần+Tháng (mockup A2 gốc chỉ có Ngày+Tuần). Việc viết MỚI hoàn toàn cả tầng đọc dữ liệu lẫn tầng vẽ — CHƯA bắt đầu, xem `sprint-status.yaml` gate 3 | planned |
| **[2026-07-15] Nghiệm thu tay lần 3 — chủ dự án tự test lần nữa, gửi 2 ảnh + 2 ý kiến** | | | | | |
| 3 icon "Bộ tiếng" căn giữa trong thẻ, không neo trái (F16) | — | ⚠️ | — | Chủ dự án: "Phần icon cho nằm ở giữa sẽ thẩm mỹ hơn." Ghi đè 1 lượt sửa TRƯỚC ĐÓ (chưa commit, phiên khác) đã cố ý đổi SANG neo trái với lý do "căn giữa trông như đảo riêng". Không phải luật DESIGN.md — chủ dự án quyết cuối cùng trên sản phẩm mình. ĐÃ VÁ: gộp công thức vào 1 hàm `bellRowStartX` dùng chung cho `relayout:` + `updateBellIndicatorAnimated:` (trước đây 2 hằng số lặp lại kèm comment "phải khớp" — nay 1 nguồn duy nhất). build sạch · make test xanh · brand-lint 0 · `git diff core/` rỗng. CHƯA ai nhìn app thật | implemented *(build-verified, chưa device)* |
| Card Gác cổng + nhật ký cảm xúc hoàn thiện ở "Hôm nay" (Epic 3 gate 3) | — | ⚠️ | ❌ | ~~planned~~ **CẬP NHẬT cùng ngày:** chủ dự án tái xác nhận thiếu, rồi yêu cầu "lên kế hoạch triển khai" → shard 4 story (3.5-3.8, skill bmad-epics-and-stories) → yêu cầu "triển khai làm khắc phục luôn" → cả 4 story CODE XONG cùng lượt (xem 4 dòng ngay dưới). CHƯA ai nhìn app thật | implemented *(build-verified, chưa device)* |
| **[2026-07-15] Epic 3 gate 3 shard — 4 story 3.5-3.8, code xong cùng 1 lượt sau khi chủ dự án chốt "triển khai luôn"** | | | | | |
| Card Gác cổng mount vào pane "Hôm nay" cửa sổ Cài đặt (Story 3.5) | — | ⚠️ | ❌ | `GatekeeperCardView` (đã chạy tốt ở popover) mount lại nguyên vào `SettingsWindowController.mm` (`mk_buildTodayPane`), LUÔN trên cùng trước sông (HIẾN CHƯƠNG §5 điều 10). Soát trước: xác nhận `GatekeeperCardView.mm` không có static/singleton state → an toàn có 2 instance sống song song (popover + cửa sổ). `mk_refreshTodayPane` gọi `-refresh` mỗi lần chọn mục. build sạch · make test 16/16 · brand-lint 0 · `git diff core/` rỗng. CHƯA ai nhìn app thật | implemented *(build-verified, chưa device)* |
| Màn Soi lại vẽ sông thật + gỡ số thô "Đỉnh gợn hôm nay" (Story 3.6) | — | ⚠️ | ❌ | `EmotionRiverView` tái dùng ngay trong `ReflectionScreenMac_Show()` thay NSTextField phẳng. Số thô "%.2f" thay bằng câu mô tả buổi (sáng/trưa/chiều/tối) tính từ `peakTs` — biến đã tính đúng ở story 2.4 nhưng **0 nơi dùng** (cùng lớp bug F14). Ngưỡng "không có gợn rõ" tái dùng đúng `0.4` đã có sẵn cho "êm", không thêm hằng số mới. Cửa sổ giãn thêm đúng `riverH+12`; nhịp 2/3/4 giữ NGUYÊN vị trí tuyệt đối gốc (2 hiệu ứng — cửa sổ giãn + nhịp 1 ăn hết phần giãn — triệt tiêu nhau). build sạch · make test 16/16 · brand-lint 0 · `git diff core/` rỗng. CHƯA ai nhìn app thật. **[2026-07-16] VIẾT LẠI theo artifact "Vòng Soi lại" chủ dự án gửi sau khi xem bản đầu** ("vẫn là pop-up cũ"): 3 nhịp Nhận ra/Soi/Nuôi dưỡng thay 4 nhịp (nhịp "Cho phép" — dòng "Quãng lặng: N phút" — gộp vào đoạn quan sát 2 câu tường thuật theo buổi), đường ngăn, eyebrow hoa, caption phụ dưới câu hỏi, thẻ gợi ý nền cam nhạt + nút "Để sau", chân trang 3 dòng tin cậy, tiêu đề cửa sổ thứ+ngày thật. `EmotionRiverView` thêm `setCaptionHidden:` (method mới, không đổi hợp đồng khoá) để tránh 2 câu quan sát chồng nhau. build sạch · test 16/16 · brand-lint 0 · `git diff core/` rỗng | implemented *(build-verified, chưa device — bản vá 2026-07-16)* |
| Xem sông theo TUẦN — Ngày/Tuần segmented (Story 3.7) | — | ⚠️ | ❌ | `MoodStoreMac_FetchWeekSamples()` mới (7 ngày, trung bình mỗi ngày, `NSNull` thật cho ngày 0 mẫu — không nội suy). Segmented `MKDateRangeSeg` MỚI viết riêng (không tái dùng `MKSegmented` của `BellSettingsView.mm` — file đó đang có sửa đổi chưa ổn định từ phiên song song). **Lệch so với AC gốc, phát hiện lúc code:** `EmotionRiverView` chỉ có ĐÚNG 4 slot nhãn trục cố định, không nhét vừa 7 tên thứ trong tuần như dự tính ban đầu — đổi sang mốc tương đối ("7 ngày trước"→"Hôm nay"), vẫn đúng tinh thần "không dashboard". build sạch · make test 16/16 · brand-lint 0 · `git diff core/` rỗng. CHƯA ai nhìn app thật, CHƯA seed đủ 7 ngày dữ liệu thật để verify bằng mắt. **[2026-07-16] BUG THẬT phát hiện qua ảnh chụp cửa sổ Cài đặt của chủ dự án:** `riverTop` (SettingsWindowController.mm) lặp nguyên công thức `segTop`, quên trừ chiều cao segmented — sông (add SAU) vẽ đè lên `MKDateRangeSeg`, control tồn tại nhưng không ai thấy. ĐÃ VÁ (1 dòng, trừ thêm `kDateRangeSegH + kTodaySectionGap`). build sạch · test 16/16 · brand-lint 0 sau vá | implemented *(build-verified, chưa device, chưa đủ dữ liệu thật — bug hiển thị đã vá 2026-07-16)* |
| Xem sông theo THÁNG — thêm lựa chọn thứ 3 (Story 3.8) | — | ⚠️ | ❌ | Trước khi code: hỏi chốt 2 câu hỏi thiết kế "DỪNG-HỎI" tự ghi trong story (hình dạng hiển thị + độ thưa gap) — chủ dự án chọn cả 2 phương án an toàn đề xuất sẵn. `MoodStoreMac_FetchMonthSamples()` dùng CHUNG hàm private `FetchDailyAverages(numDays)` với 3.7 (viết 1 lần, không copy-paste — cả 2 story code cùng lượt). Segmented giờ đủ 3 mục Ngày/Tuần/Tháng. Đã rà kỹ: không streak/số đếm/heatmap. build sạch · make test 16/16 · brand-lint 0 · `git diff core/` rỗng. CHƯA ai nhìn app thật, CHƯA seed đủ 30 ngày dữ liệu thật. **[2026-07-16]** cùng bug hiển thị với dòng 3.7 ngay trên (segmented dùng chung bị sông che) — ĐÃ VÁ chung 1 lần | implemented *(build-verified, chưa device, chưa đủ dữ liệu thật — bug hiển thị đã vá 2026-07-16)* |
| **[2026-07-16] "Chồng ghi chú" — đọc lại ghi chú ngày cũ (`NotesHistoryMac`)** | — | ⚠️ | ❌ | Đóng nốt §2.5 DECISION-daily-note-v1 (vốn hoãn "đợt sau"); chủ dự án chốt lối = chồng ghi chú chỉ-ngày-có-viết. **Tầng dữ liệu CÓ test** (`make test-macos` Ca 6b): `FetchAllNotes` trả đúng chữ + câu hỏi kèm, sửa trong ngày không đẻ dòng thứ 2, note thiếu câu hỏi → key `question` VẮNG (không bịa). `SaveNoteForToday` +tham số `question` (lưu nguyên văn vào `mood_label`). **Tầng UI CHƯA ai nhìn:** màn `NotesHistoryMac_Show` + link "Những dòng đã viết →" mới chỉ có build sạch (**debt delta = 0, đo bằng baseline sạch 2 lượt `make clean` — 10 warning trước = 10 sau, diff rỗng**; lưu ý `git stash push -- <path>` KHÔNG cất file chưa track nên lượt đầu đo sai, phải mv file mới ra ngoài) + brand-lint 0/135 + app khởi động không crash. **Cố ý KHÔNG dùng lldb gọi hàm** để chụp màn — TEST_MATRIX này ghi rõ nó từng deadlock ở story 1.8, không đem app đang chạy của chủ dự án ra thử lại | implemented *(data-verified, UI chưa device)* |
| **[2026-07-16] Auto-purge CHỪA ô ghi (chủ dự án chốt phương án (a))** | — | ⚠️ | ❌ | Vá `MoodStoreMac_RunAutoPurgeIfNeeded`: `DELETE … WHERE ts < ?` → `… AND event_type != 'note'`. Lý do: v1 chưa có cửa đọc note ngày cũ (DECISION-daily-note-v1.md) nên chữ người dùng tự viết sẽ bị xoá sau 90 ngày mà **không ai từng đọc lại được**. Copy màn Riêng tư sửa kèm (nói rõ ô ghi được chừa) — app hứa xoá mà giữ lại còn tệ hơn. ⚠️ **KHÔNG có test bảo vệ bản vá này, đã kiểm chứng bằng RED gate:** gỡ `AND event_type != 'note'` ra thì Ca 6b VẪN XANH. Nguyên nhân cấu trúc: `SaveNoteForToday` luôn ghi mốc HÔM NAY và purge chỉ xoá `ts < now − days*86400` với `days ≥ 1`, nên note-hôm-nay luôn trong hạn ở CẢ 2 bản → muốn test thật phải có cửa tiêm-ngày (chưa làm, không tự thêm). Ca 6b chỉ chứng minh purge còn dọn được ngày cũ (không hồi quy) + 2 đường xoá note hợp lệ còn chạy. Cần 1 người đổi đồng hồ máy quá 90 ngày mà thử | implemented *(chưa có test bảo vệ — xem cột Bằng chứng)* |
| **[2026-07-16] Chuỗi nhịp lấy mẫu (gõ→nhịp→ghi→đọc) — test E2E tầng dữ liệu đầu tiên của vỏ macOS** | — | ✅ | ⚠️ | `make test-macos` mới = `tests/macos/mood_pipeline` (8 ca, link file THẬT MoodWatch/MoodStore/Bell/Nudge/MoodBuffer, cô lập kho qua `MK_TEST_STORE_DIR` + stub Keychain bằng `-D` rename): nhịp chay không bịa mẫu · gõ êm → 1 mẫu thấp · quãng lặng giữ nguyên · từ căng ("bực") → biên độ cao hơn hẳn · Flush lúc thoát · số `FetchTodaySamples` khớp caption "N nhịp chuông hôm nay" · `BellMac_ApplySettings` có lên lịch nhịp gốc. **Ca 0 tóm bug thật ngay lần chạy đầu:** các hàm ĐỌC (`FetchTodaySamples`… — popover gọi mỗi lần mở) TẠO kho rỗng + khóa AES thật trong Keychain dù người dùng chưa/không consent, trái lời hứa MoodStoreMac.h — ĐÃ VÁ tại `OpenWorkingDB` (chưa consent nào + chưa có kho → NULL, không tạo). E2E=⚠️ vì timer tick thật ≥1 phút + vẽ popover bằng mắt chưa phủ (xem tests/macos/README.md) | implemented |
| **[2026-07-16] Batch 2/3 UI redesign (`SCREEN-REFERENCE.md` §2.3) — pane "Bộ gõ ▸ Kiểu gõ" thay áo brand (batch 1 = Hệ thống+Chuông, batch 3 chưa tới)** | | | | | |
| Pane "Bộ gõ ▸ Kiểu gõ": card `applyBrandCardStyle` (bóng, lệch quy ước) → `applyThinCardStyle` (viền mảnh, khớp mọi pane brand khác) + 11 nhãn màu hệ thống `labelColor` → `[Brand charcoal]` | — | ✅ | ❌ | Soi trước khi sửa: storyboard XML (`Main.storyboard`, box `HsQ-jf-eR7`) cho thấy 11 `PillSwitch` + lưới 2 cột **đã đúng sẵn** từ Story 1.7 + 1 vá "Epic 3 (2026-07-15)" — SCREEN-REFERENCE.md/decision-log chẩn "không phải PillSwitch" là **chẩn lỗi thời/sai** (đối chiếu ảnh chụp thật bên dưới). Nên diff CHỈ nằm trong `ViewController.m` (method `applyBrandCardStyleToGroups` sửa 1 dòng + method mới `styleInputGridLabels`) — 0 dòng đụng storyboard/IBAction/global/`core/`. 11 mapping switch→biến engine đối chiếu 3 lớp (nhãn+tooltip+IBAction+`extern`) trước khi kết luận không có gì mơ hồ. Build: **baseline đo SAI lúc đầu** ("0 warning" — hoá ra do cache incremental build của phiên trước); phát hiện qua `git stash push -- <file>` + `make clean && make build` 2 lượt (unedited vs edited) → cả 2 đều 10/10 warning **giống hệt** (danh sách trùng khớp), nên debt delta = 0 thật. `make test` 16/16 xanh · `brand-lint` 0/135. **Ảnh chụp app thật** qua `screencapture -l <windowID>` (window ID xác minh bằng `CGWindowListCopyWindowInfo`, KHÔNG đoán toạ độ màn hình — lần đầu đoán toạ độ qua System Events đã chụp NHẦM cửa sổ khác, xoá ngay, đổi hẳn sang phương pháp windowID) — tab "Kiểu gõ" (card+nhãn mới, 11 switch teal on/off đúng) và tab "Gõ tắt" (đã brand hoá sẵn Story 1.8, chỉ verify wiring — không đổi) đều hiện đúng. Tab "Chuyển mã" thử 2 lần không chụp được (cửa sổ "Soi lại hôm nay" vô tình mở đè đúng toạ độ click lúc dò — tác dụng phụ của việc dò click, không phải lỗi code; cùng pattern/code với "Gõ tắt" nên rủi ro sai lệch thấp) — để ngỏ cho chủ dự án tự xem. **Chưa verify được:** gõ phím Telex/VNI thật — build ad-hoc mới thu hồi quyền Accessibility/Input Monitoring (Makefile tự in cảnh báo), cần chủ dự án cấp lại quyền thủ công trước khi bấm được; bù lại bằng `test_engine` (Telex→Unicode, không đụng bởi diff này) vẫn xanh + xác nhận diff không chạm bất kỳ IBAction/global nào | implemented |
| **[2026-07-16] Đội Windows — gói "đổ móng" (build-readiness + rebrand, KHÔNG đụng lớp cảm xúc). Máy làm việc là macOS, KHÔNG có MSVC — mọi dòng dưới đây là rà soát TĨNH, chưa build thật lần nào** | | | | | |
| `OpenKey.vcxproj`/`.vcxproj.filters`/`stdafx.h` trỏ include về `core/engine` dùng chung (5 `.cpp` + 6 header qua `..\..\..\..\..\core\engine\`) | — | — | — | Rà soát tĩnh xác nhận 14/14 file engine tồn tại đúng đường dẫn tương đối; include chéo trong `core/engine` (`Engine.h`→`DataType.h`→`platforms/win32.h` qua `#ifdef _WIN32`) tự resolve được vì MSVC tìm quoted-include cùng thư mục file gốc trước, không cần thêm `AdditionalIncludeDirectories`. `PrecompiledHeader=NotUsing` toàn bộ config nên không bắt buộc mọi `.cpp` phải mở đầu bằng `#include "stdafx.h"`. **CHƯA build thật** — chờ chủ dự án mở Visual Studio | in_progress *(static-verified only, chưa build)* |
| **[MỐC 1] LNK2019 `MoodWatch`/`Bell` — ĐÃ VÁ, chờ build-verify.** `MainControlDialog.cpp`, `OpenKey.cpp`, `SystemTrayHelper.cpp` include `MoodWatch.h`/`Bell.h` và gọi `vMoodWatch`/`MoodWatch_Init()`/`MoodWatch_Toggle()`/`Bell_Init()`/`Bell_ShowSettings()`, nhưng `MoodWatch.cpp`/`Bell.cpp` (nơi định nghĩa) không có trong `ClCompile` của `OpenKey.vcxproj` | — | — | — | **Vá (2026-07-16):** chủ dự án chốt hướng (a) → thêm `MoodWatch.cpp`/`Bell.cpp` vào `ClCompile` + `MoodWatch.h`/`Bell.h` vào `ClInclude` (`OpenKey.vcxproj` + `.vcxproj.filters`). Chỉ đụng 2 file cấu hình project, 0 dòng code, không đụng `core/`. Rà tĩnh trước khi giao: XML cả 2 file parse sạch (`ElementTree`); `PrecompiledHeader=NotUsing` cả 4 config → không bẫy C1010; cả 2 `.cpp` đã có `#include "stdafx.h"`; `IDD_DIALOG_BELL` + `IDC_CHECK_BELL`/`IDC_EDIT_BELL_*`/`IDC_CHECK_MOODWATCH` đủ trong `resource.h` + dialog đã vẽ trong `.rc`; `vOnWordCommitted` có thật ở `Engine.h:224`. **CHƯA build thật** — project đòi toolset **v143 (VS 2022)**, máy dev là macOS. Chờ chủ dự án dán Output | in_progress *(đã vá tĩnh, chưa có bằng chứng build)* |
| **[MỐC 1] Vỏ Windows compile + link sạch** | ✅ | — | — | **[2026-07-17] ĐẠT.** CI `Windows` XANH cả **Debug và Release x64** ở `3b08752` (run 29550185…) và `543ea49` (run 29550398744) — lần đầu vỏ Windows được biên dịch thành công trong lịch sử repo. Artifact `.exe` tải về được (Release 360 KB). Trước đó fail 4 vòng, KHÔNG vòng nào do code Windows: (1) `set -e` giết script trước khi kịp bắn annotation → không ai thấy lỗi; (2) `msbuild: command not found` (setup-msbuild chỉ cắm PATH cho pwsh, bước chạy bash); (3) `MSB1008` (Git Bash dịch `/t:` thành đường dẫn); (4) **gốc rễ**: việc của phiên Windows trước (vá include + rebrand, 9 file) CHƯA BAO GIỜ COMMIT — CI checkout code cũ nên `stdafx.h` đòi `../../../engine/Engine.h` đã chết | implemented |
| **[MỐC 1] Gõ Telex/VNI ra dấu đúng trên Windows** (nghiệm thu Mốc 1) | ✅ | — | ❌ | Engine PASS dưới **MSVC** trong CI Windows (5 ca Telex→Unicode, nhánh `DataType.h _WIN32`→`platforms/win32.h` — nhánh macOS CI không bao giờ chạm). Nhưng **chưa ai gõ thật qua vỏ**: CI chỉ chứng minh compile+link. Ca nghiệm thu người-thật: `docs/QA-WINDOWS.md` §3 (T1–T7) | in_progress *(chờ bằng chứng gõ thật)* |
| **[GĐ1] MoodWatch (Windows) gọi `core/mood`, chấm risk thay vì dán nhãn** | ✅ | — | ❌ | **[2026-07-17]** `543ea49` — bỏ `g_words` tự chế → `MoodBuffer`; bỏ lexicon bản-photo-thứ-3 → `SendRiskAnalyzer`; ngưỡng 0.5 + cooldown 15s + giọng chữ khớp `MoodWatchMac.mm`; phân tích dời khỏi luồng hook (tránh `LowLevelHooksTimeout` gỡ hook âm thầm); copy `wstring` khi còn trên luồng hook (bẫy `vOnWordCommitted` đã cắn iOS + macOS). CI Windows XANH → `core/mood/*.cpp` link được từ vcxproj. **Chuỗi Telex→engine→MoodBuffer→SendRiskAnalyzer đã chạy thật** (2026-07-17): `ddm`→"đm" risk 0.5507 (nhắc), `tooi giaanj`→"tôi giận" risk 0.3297 (im — dưới ngưỡng), khớp khít số đã khoá ở `test_send_risk.cpp`. Popup/cooldown trên máy thật: chưa ai nhìn — `docs/QA-WINDOWS.md` §4 (M1–M6) | implemented *(build-verified, chưa ai nhìn)* |
| Rebrand chuỗi hiển thị OpenKey→Mindful Keyboard: `OpenKey.rc` (caption dialog, VERSIONINFO, STRINGTABLE) + `MessageBox`/`ShellExecute`/tray menu trong 7 file `.cpp` | — | — | — | Sửa tay từng chuỗi (không đổi ID/class/tên file/`OpenKeyManager`/`OpenKeyHelper`/`APP_CLASS`). Grep sau-sửa xác nhận 0 chuỗi "OpenKey" còn sót ngoài: code comment, file-header copyright, `OpenKeyUpdate.exe` (tên file updater ngoài, không đổi), fanpage Facebook OpenKeyVN (cố ý giữ, xem FRICTION-LOG), `CompanyName`/`InternalName`/`OriginalFilename` VERSIONINFO (cố ý giữ, xem FRICTION-LOG). **CHƯA build/chạy để xem chuỗi có tràn control (vd nút "Tắt Mindful Keyboard" 71×20px) hay không** | in_progress *(string-edit done, chưa build/chưa nhìn UI thật)* |
| **[2026-07-17] Người dùng thật (MacBook) báo: 3 tiếng chuông mặc định không đúng bảng phát triển** | | | | | |
| Cài mới xong, chuông reo ĐÚNG tiếng mà màn Chuông đang sáng | — | ⚠️ | ❌ | **PHẢN CHỨNG từ người dùng thật, truy được ra gốc:** `vBellSoundName` chưa set (cài mới) thì **2 nơi tự đoán mặc định KHÁC nhau** — `BellSettingsView.mm:566` rơi về `SoundNameForIndex(0)`="Chuông chùa" (sáng nút), còn `BellMac.mm:55` rơi về `@"Glass"` (ping hệ thống macOS 1.65s, **chưa bao giờ nằm trong thiết kế**) → mắt thấy một đằng, tai nghe một nẻo, kẹt vậy tới khi người dùng tình cờ tự bấm 1 nút chuông (chỉ lúc đó dòng 757 mới ghi vào). Nợ bỏ quên từ chính dòng 73 sổ này: 3 `.wav` vào bundle (`d377eaf`) *để thay* Glass nhưng 2 fallback không ai đổi. **Loại trừ được đóng gói TRƯỚC khi sửa code** (nghi vấn đầu tiên của chủ dự án): mở `release-out/MindfulKey.dmg` đã ship → cả 3 `.wav` có đủ trong `Contents/Resources`; dựng app tí hon nhét đúng 3 file đó → `NSSound soundNamed:` tìm ra cả 3, `afinfo` khớp bảng (23.78s / 10.32s / 1.96s). **Vá:** hằng `kBellSoundDefaultName` (BellMac.h) = 1 nguồn sự thật cho cả lối phát lẫn lối hiện; fallback tên-lạ cũng rơi về tiếng thiết kế thay vì ping hệ thống. **KHÔNG cần migrate người dùng cũ** — "Glass" chưa bao giờ được GHI vào kho (lối ghi duy nhất là `SoundNameForIndex`, chỉ trả 3 tên Việt), nó chỉ sống như fallback lúc đọc → vá fallback là mọi máy tự đúng. Bằng chứng sau vá: `make build` sạch (10 warning = baseline, không thêm) · `make test` 16/16 xanh · `make test-macos` xanh · `brand-lint` 0/156 · **soi thẳng binary vừa build**: chuỗi "Glass" = **0** dấu vết (cả utf8 lẫn utf16), 3 tên chuông có đủ (utf16, mỗi tên 1 lần — chữ có dấu compiler lưu UTF-16 nên `strings` không thấy, phải dò byte) · 3 `.wav` trong `.app`. **CHƯA ai NGHE thật** — cần 1 lỗ tai xác nhận (xem ghi chú dưới bảng) | implemented *(build+binary-verified, chưa ai nghe)* |
| Người dùng tự nạp tiếng chuông riêng (ô thứ 4 "Bộ tiếng") | ✅ | ⚠️ | ❌ | **[2026-07-17]** Chủ dự án chốt hình hài = **icon thứ 4 trong hàng** (đè story 2.5 "KHÔNG thêm ô thứ 4" + SCREEN-REFERENCE §2.2 — cả 2 file đã dán banner SUPERSEDED cùng lượt, xem decision-log). **CÓ test thật** (`make test-macos` Ca 8, 9 ca): `BellMac_SoundIdFromStored` dịch đúng dữ liệu ĐỜI CŨ (`"Chuông chùa"`→`temple`, `"Chuông gió"`→`chime`, `"Chuông reo"`→`wind`) nên **người đang dùng không mất tiếng đã chọn** khi kho đổi sang id tiếng Anh; rỗng/rác (`"Glass"`) → tiếng thiết kế; `custom`/`__silent__` giữ nguyên. **Verify RED thật:** gỡ dòng dịch `"Chuông gió"` → đúng ca đó SAI, khôi phục → xanh lại. ⚠️ **Ca `"Chuông chùa"`→`temple` là TỰ THOẢ** (mặc định cũng là `temple`) nên nó KHÔNG bắt được lỗi nếu dòng dịch đó bị gỡ — 2 ca gió/reo mới có răng. **CHƯA có test:** `BellMac_InstallCustomSound` (chép tệp + dội cache) — kho ghi thẳng Application Support THẬT, chưa có hàm dùng chung "thư mục app" để cô lập, cố ý không chép nhánh test của MoodStore sang (xem FRICTION-LOG 2026-07-17). Bằng chứng còn lại: build sạch (0 warning mới ở 3 file đụng tới) · `make test` toàn xanh · brand-lint 0/158 · asset `bell-idle.tiff` có trong `.app` (sinh từ `brand/svg/bell-idle.svg` qua `export.sh`, không vẽ tay) · binary có đủ id `temple`/`chime`/`wind`/`custom` + khoá `vBellCustomSoundPath`. **CHƯA ai bấm/nghe thật** — cần người mở tab Chuông, bấm ô thứ 4, chọn 1 tệp và nghe | implemented *(logic có test, UI+tai chưa)* |
> **Vì sao macOS=⚠️ chứ không phải ✅ cho 3/4 cửa sổ trên:** sandbox agent không có quyền
> Accessibility → không tự động click được, và gọi thẳng AppKit qua `lldb` từng gây deadlock 1 lần
> (thử ở story 1.8) nên các story sau tránh dùng — verify bằng build sạch + `make test` + `ibtool`
> + app khởi động không crash, KHÔNG phải bằng mắt thấy UI chạy đúng. Cửa sổ Convert là ngoại lệ vì
> lldb-gọi-hàm áp dụng thành công trước khi phát hiện rủi ro deadlock ở story sau.

**Chú thích `(?)`:** changelog CLAUDE.md ghi các bước này "xong / build sạch", nhưng
Bằng chứng hiện = `none`. Dấu `(?)` là lời nhắc trung thực: cần một lần chạy thật để
nâng từ *"đã code"* lên *"đã chứng minh"*.

**Chú thích ¹/² (khối Đội iOS 2026-07-11):** `✅¹` = build-verify thật (`xcodebuild` compile+link
thành công cho iOS Simulator, không phải suy đoán) nhưng **CHƯA có người chạy app quan sát hành
vi** — build sạch không đồng nghĩa hành vi đúng (đúng bài học "MoodWatchMac_Init() chưa từng được
gọi" ở đầu file này). `⚠️²` = có 1 lần cài+mở trên Simulator (không crash, UI render đúng), gần
nhất với "có người nhìn" trong đợt nhưng chưa đi hết luồng.

## Đọc bảng này thấy gì (tính đến 2026-07-10)

- Bộ não có unit test (5 case), nhưng **gần như toàn bộ lớp mood + gác cổng đang
  `implemented`/`in_progress` mà Bằng chứng = none, E2E = none.**
- Lỗ hổng lớn nhất: **hành vi gác cổng** (Feature #1) — trái tim sản phẩm — chưa có
  một bằng chứng E2E nào.
- Đợt thay áo NOW BRAND OS cho 4 cửa sổ legacy macOS (2026-07-10) đúng lỗ hổng bảng này chuyên
  bắt: **4/5 dòng mới đều `implemented` với macOS=⚠️/E2E=❌** — build sạch, test/ibtool xanh,
  nhưng chưa ai *nhìn thấy* UI chạy đúng ngoài đời (sandbox không có quyền Accessibility). Việc
  còn lại không phải code — là 1 người mở app thật, liếc qua 4 cửa sổ, nâng E2E lên ✅.
- **Đội iOS — Round 1 (2026-07-11):** đảo ngược hoàn toàn so với 2026-07-10 — 8/8 story code
  xong, mọi cổng tự động xanh (`make test-core`/`make test-ios`/`make build` macOS/build app iOS
  đều exit 0), `git diff core/` rỗng. NHƯNG bảng trên cố ý dùng `✅¹`/`⚠️²` thay vì `✅` trần cho
  hầu hết dòng: **build-verify KHÔNG phải device-verify**. Chưa một ai gõ "vieetj" trong Notes/Zalo
  thật, chưa đo RAM, chưa đi hết luồng onboarding bằng tay — đây chính là lỗ hổng mà file này tồn
  tại để bắt (đừng lặp lại kiểu `implemented (?)` ở khối mood macOS phía trên). Việc còn lại KHÔNG
  phải code — là chủ dự án mở Simulator, gõ thử, đo RAM, nâng các dòng ✅¹/⚠️² lên ✅ thật.
  Mandate vẫn hẹp: **nhật ký + nhắc thụ động**, KHÔNG gác cổng gửi tin (sandbox chặn, `retired`).
  Con sóng/chuông (Round 2) chặn ở quyết định nhận diện Q1–Q3, chưa khởi công.

- **Đội macOS — nghiệm thu tay lần 1 (2026-07-15): file này vừa chứng minh nó tồn tại để làm gì.**
  Cả Epic 2 (6 bước v2) từng ghi `✅` ở cột macOS với bằng chứng "build sạch + `make test` xanh +
  `brand-lint` 0 lỗi" — mọi cổng tự động đều xanh. Chủ dự án mở app thật xem 5/6 mục nav: **10
  finding**, trong đó một mục nav **trắng trơn** (Hệ thống), Feature #1 **không có mặt** ở pane
  "Hôm nay", nội dung **cắt cụt** ở 2 mục, và 1 nghi phạm hiến chương (chấm cam) mà `brand-lint`
  không bắt. Bài học đóng đinh: **build-verified không nói gì về việc màn hình có giống thiết kế
  hay không** — nó chỉ nói code hợp lệ. Từ Epic 3 trở đi, bằng chứng nhận `✅` cột macOS phải là
  **mắt nhìn app thật**; build-verified xuống hạng `⚠️`. Chi tiết:
  `bmad-output/macos/ACCEPTANCE-v2-2026-07-15.md`.

## Khi nào cập nhật file này

- Hoàn thành/đổi một hành vi → cập nhật dòng tương ứng, điền Bằng chứng thật.
- Có test/chạy thật mới → nâng ô `❌`/`⚠️` lên `✅` kèm đường dẫn bằng chứng.
- Bỏ một hành vi → đổi Trạng thái sang `retired`, **không xóa dòng** (giữ vết).
- Gọi qua câu nói tự nhiên tới orchestrator: *"soi bằng chứng test"*, *"ma trận test
  còn đúng không"*, *"cập nhật test matrix"*.
