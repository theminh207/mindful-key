# FRICTION-LOG — sổ ghi "chỗ phải đoán"

> Mỗi khi một agent (hoặc chủ dự án) phải **suy diễn vì thiếu luật / thiếu nguồn sự
> thật**, ghi lại đúng chỗ đó thành một dòng. Đây không phải nhật ký lỗi — nó là
> danh sách *"chỗ nào trong dự án còn mơ hồ nên AI phải đoán"*, tức là **danh sách
> việc nên viết luật / chốt quyết định tiếp theo**.
>
> Viết **cụ thể**. Tốt: *"install script không copy docs Phase 2, nhưng chuyện đó
> ngoài phạm vi"*. Kém: *"docs khó hiểu"*. Càng cụ thể càng dễ chốt.

## Cách dùng

- **Khi nào thêm dòng:** bất cứ lúc nào phải đoán một quy tắc, một đường dẫn, một ý
  đồ sản phẩm mà đáng lẽ phải có sẵn nhưng không tìm thấy.
- **Trạng thái:** `mở` (chờ chốt) · `đã chốt` (đã viết luật/quyết định, kèm nơi ghi).
- **Không xóa dòng đã chốt** — giữ vết để biết dự án từng mơ hồ ở đâu.

## Bảng

| Ngày | Việc | Chỗ phải đoán (cụ thể) | Đã tạm xử sao | Cần chốt gì | Trạng thái |
|---|---|---|---|---|---|
| 2026-07-08 | Cấu trúc thư mục nguồn | Hiến chương §3.2 nói cấu trúc đích là `Sources/MindfulKeyboard/` + XcodeGen, nhưng bản gốc là `OpenKey/Sources/OpenKey/…` + `.xcodeproj` commit sẵn | **Đã migrate** trong repo `mindful-key`: `core/engine` + `core/mood` (bộ não C++ dùng chung) + `platforms/<os>/` (vỏ native) + XcodeGen (`platforms/apple/project.yml`, `make generate`). Tên thư mục khác §3.2 (`core/`+`platforms/` thay vì `Sources/MindfulKeyboard/`) nhưng đúng tinh thần: modular + XcodeGen thay `.xcodeproj` commit | Đã chốt cấu trúc `core/`+`platforms/`. (Việc còn lại: `.claude/` chưa theo path mới — xem dòng 2026-07-10 dưới) | đã chốt |
| 2026-07-08 | Thang màu brand cho cảm xúc | Một số asset đang dùng thang teal→cam-bão-hòa để mã hóa cảm xúc, khác luật 2.3 (nhận diện theo **biên độ** + sắc độ **trung tính, không bão hòa**) | Giữ asset hiện tại, chưa soát lại | Chủ dự án chốt: soát lại toàn bộ asset theo 2.3 (biên độ, không dùng màu bão hòa để chấm điểm cảm xúc) | mở |
| 2026-07-10 | Gác cổng Feature #1 trên iOS | iOS keyboard extension bị sandbox: không thấy nút Gửi host app, không tap toàn cục như `CGEventTap` macOS → **không chặn được** gửi tin. Android chỉ chặn được phím action khi `imeOptions=actionSend`. Feature #1 hoá ra không đồng đều giữa 3 OS | **Chủ dự án đã chốt (2026-07-10):** iOS chấp nhận giới hạn sandbox — mandate hẹp lại còn **nhật ký cảm xúc + nhắc chánh niệm thụ động** (gợn sóng `~` ambient), KHÔNG chặn Enter xuyên app. Nhận diện "người gác cổng" giữ ở macOS/Windows; iOS thể hiện cùng tinh thần chánh niệm ở mức "quan sát + nhắc", không "chặn". Ghi ở: TEST_MATRIX (khối Đội iOS, dòng gác cổng iOS = `retired`), `platforms/apple/ios/README.md`, agent `ios-shell-agent`, skill `ios-keyboard-extension` | Đã chốt mandate iOS = nhật ký + nhắc thụ động | đã chốt |
| 2026-07-10 | `.claude/` (agents + skills) còn path cũ | `engine-agent.md`, `platform-shell-agent.md`, skill `platform-porting`/`openkey-engine` vẫn nói path bản gốc (`OpenKey/Sources/OpenKey/engine/`, `win32/`, `macOS/`) trong khi repo con đã đổi sang `core/engine`, `platforms/apple/macos`, `platforms/windows/` | File mới/đụng lần này (harness orchestrator, `ios-shell-agent`, `ios-keyboard-extension`, phần routing) dùng path đúng của con; các file cũ chưa đụng vẫn giữ path cũ để không sửa ngoài phạm vi yêu cầu | Chủ dự án chốt: có nên quét đổi hết path cũ trong `.claude/agents` + `.claude/skills/{openkey-engine,platform-porting}` sang path `core/`+`platforms/` không? | mở |
| 2026-07-12 | Onboarding iOS — lối tiến khi App Group hỏng | Màn 01→02 CHỈ tiến qua nhịp tim App Group (container đọc lúc foreground, story 1.6/AppDelegate). KHÔNG có nút "tiếp tục" thủ công. Phát hiện khi test tay: App Group đọc rỗng → người dùng **kẹt Màn 01 vĩnh viễn**, nút "Mở Cài đặt" chỉ quay vòng. Đây là single-point-of-failure cho toàn bộ onboarding | **ĐÃ VÁ (2026-07-13, commit `5b225c3`):** thêm lối tiến thủ công êm — nút "Đã thêm xong — tiếp tục" HÉ RA sau khi người dùng đã rời sang Cài đặt rồi quay lại mà heartbeat không nhảy (ẩn ban đầu để không khuyến khích bỏ qua bước bật thật). Heartbeat vẫn là đường auto chính; đây là fallback | Đã chốt hướng: onboarding CÓ lối thoát thủ công (không kẹt vĩnh viễn). Chủ dự án đã uỷ quyền tự hoàn thành; có thể veto/đổi giọng copy sau. Ghi ở: commit `5b225c3` + `ActivationViewController` | đã chốt |
| 2026-07-12 | Kiểm App Group heartbeat trên Simulator | Entitlement App Group KHÔNG nhúng vào binary iOS Simulator với cấu hình ký hiện tại (`CODE_SIGN_STYLE: Manual` + `CODE_SIGN_IDENTITY: "-"` + `CODE_SIGNING_ALLOWED: NO`, không team). `codesign -d` không thấy group; `NSUserDefaults(suiteName:)` trả rỗng; gieo giá trị vào cả shared-container lẫn app-container đều không được app đọc. → detect "đã kích hoạt" không chạy được trên Simulator | Xếp heartbeat detection (story 1.6, FR-A06) vào nhóm **device-only** cùng Zalo/RAM (Phần D `tests/ios/MANUAL-TEST-SCRIPT.md`). Gõ Telex + UI + Shift/số vẫn test đủ trên Simulator | (Kỹ thuật, không gấp) muốn chỉnh cấu hình ký để entitlement nhúng được trên Simulator (test App Group tại chỗ), hay chấp nhận App Group chỉ verify trên máy thật? | mở |
| 2026-07-13 | Analyzer send-risk cho iOS R2 | Lexicon tính `send-risk` nằm ở vỏ macOS (`MoodWatchMac.mm`), KHÔNG ở `core/mood` (`MoodBuffer` chỉ gom câu — tự ghi "shells decide how to analyze"). `core/` đóng băng với iOS nên không thêm vào đó được. Nếu iOS tự implement → **2 bản analyzer** (macOS + iOS), phạm nhẹ "1 bộ não, không fork logic" | **Chọn phương án B (tech-spec-r2 §Dependency):** iOS implement analyzer ở `platforms/apple/shared/`, TÁI DÙNG bảng lexicon rút từ `MoodWatchMac.mm` (dữ liệu thuần, tách được), KHÔNG đụng `core/`. iOS unblocked cho R2 | Chủ dự án: (1) xác nhận phương án B cho R2; (2) sau R2 có đưa analyzer lexicon vào `core/mood` (đội core) để hợp nhất "1 bộ não" không? | mở |
| 2026-07-13 | Bàn phím iOS câm khi test tay — 4 lớp lỗi + quyết định Full Access | Test tay trên Simulator: bàn phím không lên/không bung macro. Đào ra 4 lớp chồng: (1) ký `CODE_SIGNING_ALLOWED=NO` → App Group container KHÔNG được cấp (giải luôn câu hỏi mở dòng 2026-07-12); (2) Xcode 16+ tách `.debug.dylib` → app-extension bàn phím không nạp được; (3) `RequestsOpenAccess=false` từ R1 → iOS ẩn nút Full Access + chặn App Group; (4) kể cả vá đủ 3 cái trên, Simulator VẪN không dựng custom keyboard (pkd đã `Created plugin`, không crash — tật kinh điển máy ảo) | **ĐÃ VÁ (commit `fa0c988`):** run-ios ký ad-hoc KÈM entitlements + `ENABLE_DEBUG_DYLIB=NO`; `Info.plist RequestsOpenAccess=true`. App Group + macro + settings giờ verify được trên Simulator ở tầng dữ liệu. Phần **gõ thật bằng bàn phím = device-only** (chốt cùng nhóm Zalo/RAM). Chủ dự án đã chọn hướng **cắm iPhone thật** (Apple ID free đủ cho gõ + sóng + chuông; macro/App Group cần tài khoản trả phí) | **CẦN CHỦ DỰ ÁN PHÊ CHUẨN:** iOS giờ **xin Full Access** (open access) — chạm riêng tư (§hiến chương). Lý do bắt buộc: đọc câu cho con sóng + mở App Group cho macro/settings, TẤT CẢ on-device (đã ghi copy màn FullAccess + comment Info.plist). Owner có thể veto nếu muốn iOS bỏ macro/settings để khỏi xin quyền | mở |

| 2026-07-13 | Notification cuối ngày iOS (R3 Q5) vs mandate "nhắc thụ động" | Mandate iOS (2026-07-10) chốt "nhật ký + nhắc **THỤ ĐỘNG**, không hối thúc". Round 3 Q5 chủ dự án chọn CÓ 1 thông báo đẩy cuối ngày mời soi lại — notification = "nhắc **chủ động**", nghịch nhẹ mandate | **Đã chốt (2026-07-13):** giữ notification NHƯNG rào cứng — **mặc định TẮT, opt-in rõ ràng, tối đa 1 lần/ngày, giờ chỉnh được, tắt được, copy quan sát không phán xét** (CẤM kiểu "bạn bỏ lỡ N ngày"). Ghi ở `bmad-output/ios/decision-log.md` 2026-07-13 + `tech-spec-r3.md` story 3.4 | Đã chốt nới lỏng CÓ Ý THỨC (rào opt-in). Nếu về sau thấy phiền → veto, quay lại thuần-trong-app | đã chốt |
| 2026-07-13 | Full Access nặng ký hơn ở R3 | Dòng 2026-07-13 (Full Access) còn `mở`. R3 bắt đầu **PERSIST dữ liệu phái sinh** (send-risk sự kiện, mã hóa) thay vì chỉ sóng tức thời trong RAM → mức độ hệ trọng của việc "xin đọc mọi thứ gõ" tăng lên | Vẫn build R3 (chủ dự án yêu cầu) với đủ rào riêng tư: on-device, mã hóa AES+Keychain, không văn bản gốc, consent-gate, log tối thiểu (chỉ tense-event). KHÔNG tự đóng dòng Full Access | Chủ dự án chính thức khẳng định/đóng dòng Full Access (2026-07-13) khi đã thấy R3 persist gì | mở |
| 2026-07-13 | Giá trị `kSecAttrAccessGroup` cho khóa Keychain chia sẻ (story 3.1) | Kho nhật ký iOS cần extension (ghi) + container (đọc) dùng CHUNG 1 khóa AES qua `keychain-access-groups`. Runtime phải set `kSecAttrAccessGroup` khớp entitlement `$(AppIdentifierPrefix)vn.gnh.mindfulkey.shared`, NHƯNG `$(AppIdentifierPrefix)` chỉ resolve lúc KÝ THẬT bằng Team ID — dự án ký ad-hoc không có Team ID nên KHÔNG biết giá trị đã resolve lúc biên dịch | **Tạm xử:** hardcode `@"vn.gnh.mindfulkey.shared"` (không tiền tố) + bọc `#if TARGET_OS_IPHONE` để host test bỏ qua field này (dùng keychain mặc định, vẫn test được crypto/consent). Ghi rõ device-only ở `MoodJournalStore.mm` header + TEST_MATRIX | Khi có Apple Developer Program (Team ID thật): verify access group khớp trên máy thật; nếu `errSecMissingEntitlement` → chỉnh giá trị/tiền tố cho đúng | mở |
| 2026-07-14 | Phạm vi cột CSV export (story 2.6) | Story 2.6 tự ghi rõ đây là "DỪNG-HỎI chủ dự án" (2 cách hiểu: xuất y hệt dữ liệu đã lưu gồm `app_bundle_id`/`choice`, hay xuất hẹp hơn chỉ điểm+giờ+mood) — nhưng dev agent thi công đã TỰ CHỌN bản hẹp (comment code "Xuất hẹp... bỏ qua app_bundle_id và choice") và code chạy thẳng, không dừng lại chờ xác nhận, không ghi vào `Dev Agent Record` của story, không tạo dòng nào ở đây. Bị bỏ sót tới 2026-07-14 mới phát hiện lúc soát lỗ hổng quy trình theo dõi kế hoạch | **Đã chốt (2026-07-14, xác nhận qua hội thoại với chủ dự án):** giữ bản hẹp như code đang chạy — hẹp hơn dữ liệu lưu tại chỗ là lựa chọn ĐÚNG cho dữ liệu rời khỏi vùng mã hoá. Ghi ở `decision-log.md` entry 2026-07-14 "Chốt điểm gai #3" + cập nhật `docs/PRIVACY-NOTE.md` | Không còn — bài học rút ra: câu "DỪNG-HỎI" viết trong file story KHÔNG tự động nghĩa là có ai hỏi thật; PHẢI kèm 1 dòng ở đây thì mới nổi lên được | đã chốt |
| 2026-07-14 | Mặc định N-ngày tự xoá nhật ký (story 2.6) | Không nguồn nào chốt sẵn con số; dev agent tự đề xuất 90 ngày (`[Inference]`) và code luôn theo số đó, không dừng chờ xác nhận (mức độ nhẹ hơn dòng CSV ở trên — story tự ghi "không cần dừng hẳn") | **Đã chốt (2026-07-14, xác nhận qua hội thoại với chủ dự án):** giữ 90 ngày mặc định + 4 lựa chọn 30/60/90/Không bao giờ như code đang chạy | Không còn | đã chốt |
| 2026-07-15 | "Hôm nay" ở cửa sổ vs ở popover (nghiệm thu F2) | Có HAI chỗ tên "Hôm nay": tab trong popover (có card Gác cổng + SensitivityCard, chạy thật) và mục nav trong cửa sổ quản lý (chỉ có tiêu đề + sông rỗng). Mockup A2 vẽ card gác cổng NẰM TRONG cửa sổ. Không nguồn nào nói 2 chỗ này quan hệ thế nào — bản đầy đủ vs bản rút gọn? gương của nhau? cố ý khác vai? | **Đã chốt (2026-07-15, hỏi thẳng chủ dự án trước khi code — đúng bài học "Chốt điểm gai #3"):** "Hôm nay" ở cửa sổ = **bản ĐẦY ĐỦ** (card Gác cổng + sông + lối vào Soi lại); popover giữ bản gọn để liếc nhanh. Khớp mockup A2. Ghi ở `decision-log.md` 2026-07-15 "Chốt 3 câu chặn Epic 3" | Không còn — Epic 3 gate 3 hết chặn | đã chốt |
| 2026-07-15 | Nhịp chuông 15 phút vs trần lấy mẫu đã chốt (nghiệm thu F10) | Artifact A2 chốt nhịp lấy mẫu = **30 hoặc 60** phút → "cả ngày chỉ ~24–48 chấm". App thật có thêm mức **15** (đang chọn mặc định) → ~96 chấm/ngày, gấp đôi trần. Chuông = nhịp lấy mẫu (chốt 2026-07-13) nên đây không chỉ là chuyện âm thanh — nó là **tần suất ghi nhật ký**, tức chạm cột trụ riêng tư ("ghi nhiều hơn hiện tại") | **Đã chốt (2026-07-15):** 2 nút nhanh **30/60** + ô **"Tùy chỉnh"** điền số phút, **sàn 15 / trần 240**. Sàn tồn tại vì chuông gánh 2 vai: reo mỗi 5 phút = **hối thúc** (HIẾN CHƯƠNG cấm), và nhịp dày = nhật ký thành **dòng thời gian cảm xúc chi tiết**, phá trần riêng tư. Sàn = 15 vì đúng bằng mức thấp nhất đang chạy. Ghi ở `decision-log.md` 2026-07-15 | **Còn 1 việc kéo theo:** `docs/PRIVACY-NOTE.md` đang hứa "~24–48 chấm/ngày" — chỉ đúng cho 30/60. Đặt 15 → ~96 chấm. Phải sửa thành khoảng thật | đã chốt *(còn việc kéo theo)* |
| 2026-07-16 | `make build` và `make run` xuất ra HAI nơi khác nhau — bẫy "vá rồi mà vẫn sập" | `make build` chạy `xcodebuild` KHÔNG kèm `-derivedDataPath` → sản phẩm rơi vào `~/Library/Developer/Xcode/DerivedData/MindfulKey-<hash>/`. `make run` thì CÓ kèm `-derivedDataPath platforms/apple/build` → sản phẩm rơi vào trong repo. Hệ quả: (a) `make run` build **2 lần** (vì `run: build`), ra 2 bản ở 2 nơi; (b) ai `make build` rồi `open platforms/apple/build/...` sẽ chạy **bản CŨ**, tưởng đã vá mà thật ra chưa. **Đã cắn thật 2026-07-16:** agent vá crash `MKRiverCanvas` lúc 00:59 (vào DerivedData), rồi mở nhầm bản 00:37 ở `platforms/apple/build`, tuyên bố "đã vá, chạy ổn định" — app vẫn sập lúc 01:02 đúng lúc chủ dự án thử mở. Mất ~30 phút + 1 lần báo sai cho chủ dự án | **Đã chốt + đã vá (2026-07-16, cùng phiên):** chọn phương án (1) — thống nhất mọi thứ về `$(DERIVED)`. `build` nay kèm `-derivedDataPath "$(DERIVED)"` (nên `make clean` dọn được thật); `run` bỏ lần build thứ 2, đổi thành **build → cài `/Applications` → mở**, để bản đem test LUÔN là bản Spotlight/Finder mở; thêm `make doctor` liệt kê bản lạc + bản đang chạy. `scripts/build-dmg.sh` đã học layout mới (`build/Build/Products/<config>/`), nhánh dò DerivedData hạ xuống cuối + ghi rõ là dự phòng cho bản build bằng Xcode GUI. CI không đụng (nó gọi `xcodebuild` thẳng, không qua `make build` — đã kiểm) | Không còn | đã chốt |
| 2026-07-15 | Tên sản phẩm hiển thị (nghiệm thu F9) | Tiêu đề cửa sổ thật: **"Cài đặt Mindful Keyboard"**. Nhưng artifact A2 + repo (`mindful-key`) + DMG (`MindfulKey-0.2.0.dmg`) + wordmark brand đều gọi **"mindful-key"**. Repo cha thì tên `mindful-keyboard`. Ba cách gọi cùng tồn tại, không nguồn nào chốt cái nào là tên hiển thị cho người dùng | **Đã chốt (2026-07-15):** tên hiển thị cho người dùng = **"Mindful Key"** (2 chữ, viết hoa). Khớp wordmark + repo + DMG. Tiêu đề cửa sổ "Cài đặt Mindful Keyboard" → đổi theo. Ghi ở `decision-log.md` 2026-07-15 | **Còn 1 việc kéo theo:** viết luật tên vào `docs/BRAND-ASSETS.md` + quét chữ "Mindful Keyboard" còn sót trong UI | đã chốt *(còn việc kéo theo)* |
| 2026-07-16 | Dòng sông đọc thành mấy khúc sóng rời (chủ dự án phản hồi "chưa hiểu màn Hôm nay") | Sông chỉ vẽ nước+chấm ở chỗ có mẫu (đúng dec.4 "không bịa nước giả"), nên sáng/chiều thành 2 khúc trôi rời, giữa trống → không đọc thành MỘT dòng. Muốn nối cả ngày nhưng KHÔNG được reverse dec.4 (vẽ nước phẳng = nói dối "phẳng lặng") | **Đã làm (2026-07-16), chủ dự án đã duyệt qua mockup trước khi code:** thêm **trục thời-gian nét đứt mờ (Brand stone, alpha 0.5)** chạy suốt ngày ở `EmotionRiverView.mm` — là TRỤC (cùng họ nhãn Sáng/Trưa/Chiều/Tối), KHÔNG phải mặt nước; nước teal đặc + chấm vẫn CHỈ ở mẫu thật → dec.4 nguyên vẹn. Kèm vá bug chấm tô TRẮNG (tàng hình trên thẻ trắng) → tô teal đặc 6.6px cho "vòng tròn" thật hiện. Chốt trục dọc = **chỉ cường độ, không valence**. brand-lint 0 · make build sạch · make test xanh | Chủ dự án có thể veto trục nét đứt nếu thấy vẫn giống "phẳng lặng giả"; và chọn có làm tab Tuần/Tháng cùng ngôn ngữ này không | đã chốt |
| 2026-07-16 | Bề rộng thẻ + eyebrow pane "Bộ gõ ▸ Kiểu gõ" (batch 2 UI redesign, `SCREEN-REFERENCE.md` §2.3) | §2.3 chỉ nói "1 thẻ chứa lưới PillSwitch (2 cột)" — không nói rộng bao nhiêu hay có eyebrow hay không. Thẻ hiện tại (thừa kế từ storyboard OpenKey cũ, box `HsQ-jf-eR7`) rộng 520pt, HẸP hơn `kMaxPaneW`=600pt mà mọi pane brand khác (Chuông/Riêng tư/Hệ thống) lấp đủ — để hở ~80pt trắng bên phải. Và §2.2/§2.4 đều liệt kê eyebrow tên riêng cho từng thẻ, còn §2.3 thì không nhắc | **Chọn giữ nguyên, KHÔNG tự chế:** không nới thẻ lên 600pt — muốn vậy phải tính lại toạ độ x của cả lưới 2 cột (11 switch + 11 nhãn, số point cụ thể trong storyboard), rủi ro lệch/đè chữ cao hơn lợi ích thẩm mỹ, và spec không bắt buộc. Không thêm eyebrow — nút subnav "Kiểu gõ" vừa bấm đã đóng vai trò tên nhóm, thêm "KIỂU GÕ" ngay dưới sẽ lặp chữ (khác Chuông/Riêng tư vốn nhiều nhóm trong 1 mục nav nên cần eyebrow phân biệt) | Chủ dự án xác nhận: (a) có cần nới thẻ full-width 600pt cho khớp Chuông/Riêng tư/Hệ thống không; (b) có cần thêm eyebrow phía trên thẻ Kiểu gõ không — và nếu có thì tên gì (không có sẵn trong tài liệu nào) | mở |

| 2026-07-16 | Hợp đồng vòng đời của `vOnWordCommitted` chỉ được ghi TRONG vỏ iOS | `Engine.h:224` khai báo `void (*vOnWordCommitted)(const wstring& word)` — không một chữ nào nói tham chiếu đó trỏ tới **biến cục bộ** `w` của `emitCommittedWord()` (`Engine.cpp:463`), chết ngay khi callback return. Vỏ iOS đã cắn phải rồi tự vá + ghi bài học vào comment `MoodBridge.mm:41-45` (triệu chứng của họ: `recentText()` rỗng). Kiến thức đó **nằm trong file của đội iOS**, không có ở header engine, không có ở skill/CLAUDE.md → vỏ macOS ngã đúng hố đó và **không ai biết suốt nhiều tháng**: `MoodWatchMac.mm` capture thẳng `word` vào block `dispatch_async`. Triệu chứng bên macOS nặng hơn iOS: lúc mất chữ (lexicon phân tích thiếu từ vừa gõ), lúc `abort()` (**10 crash đêm 2026-07-16, cái cuối 01:44** — backtrace: `pushWord` → `push_back` → copy ctor → `length_error`) | **Đã vá (2026-07-16):** `MoodWatchMac.mm` copy `wstring wordCopy = word;` TRƯỚC `dispatch_async`, đặt trùng tên biến với `MoodBridge.mm` để grep `wordCopy` ra cả 2 vỏ. Cơ chế được **chứng minh bằng test thật, không suy từ backtrace**: trong block, `&word` vẫn đúng ô nhớ đã chết (size 24 → 0); bản chưa vá ghi **0 từ**, bản vá ghi **1 từ**. `core/` KHÔNG đụng (nó nhận `const&` rồi tự copy — hoàn toàn đúng; lỗi 100% ở vỏ) | **Nên viết luật ở tầng engine, đừng để mỗi vỏ tự cắn 1 lần:** (a) ghi thẳng vào `Engine.h` cạnh khai báo `vOnWordCommitted` rằng tham chiếu **chỉ sống trong lúc gọi** — vỏ nào muốn dùng bất đồng bộ PHẢI tự copy; (b) vỏ Windows/Linux sau này sẽ gặp y hệt — có cần dòng này trong skill `platform-porting` không? Sửa `Engine.h` = chạm `core/` đang đóng băng với đội iOS → cần chủ dự án chốt | mở |

| 2026-07-16 | Cô lập kho cho test host macOS — env `HOME` KHÔNG ăn | Viết test E2E `tests/macos/mood_pipeline` cần cô lập `mood.enc` khỏi kho thật. Cách hiển nhiên (tests/ios từng dùng tư duy tương tự): set env `HOME` = thư mục tạm. **Verify thực nghiệm: KHÔNG ăn** — `NSHomeDirectory()`/`URLForDirectory:` trên macOS lấy home qua `getpwuid`, phớt lờ `$HOME` (in ra `/Users/now` dù `getenv("HOME")` = thư mục tạm). Không tài liệu nào trong repo cảnh báo | **Đã xử (2026-07-16):** thêm nhánh test-only vào `SupportDirectoryURL()` (MoodStoreMac.mm) đọc env `MK_TEST_STORE_DIR`, bọc `#ifdef MK_TEST_STORE_DIR_ENV` — CHỈ script test định nghĩa macro, binary app thật không có nhánh này. Keychain thì cô lập bằng `-D` rename 3 hàm `SecItem*` sang stub (khỏi đụng khóa thật + khỏi hộp thoại xin quyền treo CI). Test tự abort nếu env không trỏ thư mục tạm | Không còn — bài học ghi ở `tests/macos/README.md` cho test host sau này | đã chốt |
| 2026-07-16 | File nháp của phiên song song rơi vào `platforms/apple/macos/` làm gãy `make build` | Trong lúc phiên này chạy test E2E (20:45–20:46), một phiên khác thả `test_enabled.m` + `test_hit.m` (mỗi file 1 `main()` nháp probe NSControl) + `a.out` vào THẲNG thư mục nguồn vỏ macOS. XcodeGen glob `*.m` → 3 symbol `_main` trùng → `make build` FAIL toàn cục. Phiên này KHÔNG xóa (file của đội khác, quy ước 2 đội chung tree) — nhưng ai chạy preflight lúc đó sẽ tưởng lỗi của mình | Bản vá MoodStoreMac.mm của phiên này được chứng minh biên dịch+link sạch qua chính binary test E2E (không cần full build). Báo chủ dự án trong phiên | Chủ dự án: (a) dọn/di dời 3 file nháp (`test_enabled.m`, `test_hit.m`, `a.out`); (b) có nên thêm luật "file nháp/probe CẤM để trong `platforms/` — dùng scratchpad" vào CLAUDE.md không? | mở |

## Ghi chú

- File này bổ cho `TEST_MATRIX.md`: **ma trận** phơi *cái gì chưa được chứng minh*,
  **friction-log** phơi *chỗ nào AI phải đoán* — hai góc mù khác nhau.
- Khi một dòng chuyển sang `đã chốt`, ghi rõ luật/quyết định nằm ở đâu (vd: "đã cập
  nhật `docs/AGENT-BRIEF.md §3.2`" hoặc "changelog CLAUDE.md ngày …").
