# FRICTION-LOG — sổ ghi "chỗ phải đoán"

> Mỗi khi một agent (hoặc chủ dự án) phải **suy diễn vì thiếu luật / thiếu nguồn sự
> thật**, ghi lại đúng chỗ đó thành một dòng. Đây không phải nhật ký lỗi — nó là
> danh sách *"chỗ nào trong dự án còn mơ hồ nên AI phải đoán"*, tức là **danh sách
> việc nên viết luật / chốt quyết định tiếp theo**.
>
> Viết **cụ thể**. Tốt: *"install script không copy docs Phase 2, nhưng chuyện đó
> ngoài phạm vi"*. Kém: *"docs khó hiểu"*. Càng cụ thể càng dễ chốt.

## Cách dùng

- **Khi nào thêm dòng:** bất cứ lúc nào phải đoán một quy tắc, một đường dẫn, một ý
  đồ sản phẩm mà đáng lẽ phải có sẵn nhưng không tìm thấy.
- **Trạng thái:** `mở` (chờ chốt) · `đã chốt` (đã viết luật/quyết định, kèm nơi ghi).
- **Không xóa dòng đã chốt** — giữ vết để biết dự án từng mơ hồ ở đâu.

## Bảng

| Ngày | Việc | Chỗ phải đoán (cụ thể) | Đã tạm xử sao | Cần chốt gì | Trạng thái |
|---|---|---|---|---|---|
| 2026-07-08 | Cấu trúc thư mục nguồn | Hiến chương §3.2 nói cấu trúc đích là `Sources/MindfulKeyboard/` + XcodeGen, nhưng bản gốc là `OpenKey/Sources/OpenKey/…` + `.xcodeproj` commit sẵn | **Đã migrate** trong repo `mindful-key`: `core/engine` + `core/mood` (bộ não C++ dùng chung) + `platforms/<os>/` (vỏ native) + XcodeGen (`platforms/apple/project.yml`, `make generate`). Tên thư mục khác §3.2 (`core/`+`platforms/` thay vì `Sources/MindfulKeyboard/`) nhưng đúng tinh thần: modular + XcodeGen thay `.xcodeproj` commit | Đã chốt cấu trúc `core/`+`platforms/`. (Việc còn lại: `.claude/` chưa theo path mới — xem dòng 2026-07-10 dưới) | đã chốt |
| 2026-07-08 | Thang màu brand cho cảm xúc | Một số asset đang dùng thang teal→cam-bão-hòa để mã hóa cảm xúc, khác luật 2.3 (nhận diện theo **biên độ** + sắc độ **trung tính, không bão hòa**) | Giữ asset hiện tại, chưa soát lại | Chủ dự án chốt: soát lại toàn bộ asset theo 2.3 (biên độ, không dùng màu bão hòa để chấm điểm cảm xúc) | mở |
| 2026-07-10 | Gác cổng Feature #1 trên iOS | iOS keyboard extension bị sandbox: không thấy nút Gửi host app, không tap toàn cục như `CGEventTap` macOS → **không chặn được** gửi tin. Android chỉ chặn được phím action khi `imeOptions=actionSend`. Feature #1 hoá ra không đồng đều giữa 3 OS | **Chủ dự án đã chốt (2026-07-10):** iOS chấp nhận giới hạn sandbox — mandate hẹp lại còn **nhật ký cảm xúc + nhắc chánh niệm thụ động** (gợn sóng `~` ambient), KHÔNG chặn Enter xuyên app. Nhận diện "người gác cổng" giữ ở macOS/Windows; iOS thể hiện cùng tinh thần chánh niệm ở mức "quan sát + nhắc", không "chặn". Ghi ở: TEST_MATRIX (khối Đội iOS, dòng gác cổng iOS = `retired`), `platforms/apple/ios/README.md`, agent `ios-shell-agent`, skill `ios-keyboard-extension` | Đã chốt mandate iOS = nhật ký + nhắc thụ động | đã chốt |
| 2026-07-10 | `.claude/` (agents + skills) còn path cũ | `engine-agent.md`, `platform-shell-agent.md`, skill `platform-porting`/`openkey-engine` vẫn nói path bản gốc (`OpenKey/Sources/OpenKey/engine/`, `win32/`, `macOS/`) trong khi repo con đã đổi sang `core/engine`, `platforms/apple/macos`, `platforms/windows/` | File mới/đụng lần này (harness orchestrator, `ios-shell-agent`, `ios-keyboard-extension`, phần routing) dùng path đúng của con; các file cũ chưa đụng vẫn giữ path cũ để không sửa ngoài phạm vi yêu cầu | Chủ dự án chốt: có nên quét đổi hết path cũ trong `.claude/agents` + `.claude/skills/{openkey-engine,platform-porting}` sang path `core/`+`platforms/` không? | mở |
| 2026-07-12 | Onboarding iOS — lối tiến khi App Group hỏng | Màn 01→02 CHỈ tiến qua nhịp tim App Group (container đọc lúc foreground, story 1.6/AppDelegate). KHÔNG có nút "tiếp tục" thủ công. Phát hiện khi test tay: App Group đọc rỗng → người dùng **kẹt Màn 01 vĩnh viễn**, nút "Mở Cài đặt" chỉ quay vòng. Đây là single-point-of-failure cho toàn bộ onboarding | **ĐÃ VÁ (2026-07-13, commit `5b225c3`):** thêm lối tiến thủ công êm — nút "Đã thêm xong — tiếp tục" HÉ RA sau khi người dùng đã rời sang Cài đặt rồi quay lại mà heartbeat không nhảy (ẩn ban đầu để không khuyến khích bỏ qua bước bật thật). Heartbeat vẫn là đường auto chính; đây là fallback | Đã chốt hướng: onboarding CÓ lối thoát thủ công (không kẹt vĩnh viễn). Chủ dự án đã uỷ quyền tự hoàn thành; có thể veto/đổi giọng copy sau. Ghi ở: commit `5b225c3` + `ActivationViewController` | đã chốt |
| 2026-07-12 | Kiểm App Group heartbeat trên Simulator | Entitlement App Group KHÔNG nhúng vào binary iOS Simulator với cấu hình ký hiện tại (`CODE_SIGN_STYLE: Manual` + `CODE_SIGN_IDENTITY: "-"` + `CODE_SIGNING_ALLOWED: NO`, không team). `codesign -d` không thấy group; `NSUserDefaults(suiteName:)` trả rỗng; gieo giá trị vào cả shared-container lẫn app-container đều không được app đọc. → detect "đã kích hoạt" không chạy được trên Simulator | Xếp heartbeat detection (story 1.6, FR-A06) vào nhóm **device-only** cùng Zalo/RAM (Phần D `tests/ios/MANUAL-TEST-SCRIPT.md`). Gõ Telex + UI + Shift/số vẫn test đủ trên Simulator | (Kỹ thuật, không gấp) muốn chỉnh cấu hình ký để entitlement nhúng được trên Simulator (test App Group tại chỗ), hay chấp nhận App Group chỉ verify trên máy thật? | mở |
| 2026-07-13 | Analyzer send-risk cho iOS R2 | Lexicon tính `send-risk` nằm ở vỏ macOS (`MoodWatchMac.mm`), KHÔNG ở `core/mood` (`MoodBuffer` chỉ gom câu — tự ghi "shells decide how to analyze"). `core/` đóng băng với iOS nên không thêm vào đó được. Nếu iOS tự implement → **2 bản analyzer** (macOS + iOS), phạm nhẹ "1 bộ não, không fork logic" | **Chọn phương án B (tech-spec-r2 §Dependency):** iOS implement analyzer ở `platforms/apple/shared/`, TÁI DÙNG bảng lexicon rút từ `MoodWatchMac.mm` (dữ liệu thuần, tách được), KHÔNG đụng `core/`. iOS unblocked cho R2 | **ĐÃ CHỐT + ĐÃ LÀM (2026-07-16):** chủ dự án chọn **đưa về `core/mood` ngay, 1 bản cho cả 3 vỏ**, khi vỏ Windows sắp thành bản photo THỨ BA. Nay: `core/mood/SendRiskAnalyzer.{h,cpp}` (C++ thuần) là bản DUY NHẤT; `platforms/apple/shared/SendRiskAnalyzer.{h,mm}` đã XOÁ; `MoodBridge.mm` (iOS) + `MoodWatchMac.mm` (macOS) nay gọi chung file core; khoá bằng `tests/core/test_send_risk.cpp` (27 ca) trong `make test-core`. **Lo ngại "2 bản trôi lệch" ở cột trái KHÔNG phải giả định — nó đã xảy ra thật:** bản macOS coi dấu câu là dấu tách từ, bản `shared/` thì không → `"tôi giận."` chấm **0.329680 trên macOS nhưng 0.000000 trên iOS** (lớp cảm xúc iOS mù với mọi câu kết thúc bằng dấu câu). Bản core theo macOS (chuẩn hành vi) nên iOS được vá luôn nhân tiện. Phần hạ chữ thường CỐ Ý nằm trong core (không giao cho vỏ) — giao ra ngoài là 3 vỏ trôi lệch lần nữa; bảng tiếng Việt viết tay đã đối chiếu **89/89 chữ hoa khớp tuyệt đối** với `NSString lowercaseString` của Apple | Không còn — bài học: dòng này để `mở` 3 ngày và 2 bản đã kịp trôi lệch. "Chấp nhận có chủ đích 2 bản cho tới khi core hợp nhất" là món nợ có lãi suất, không phải nợ đứng yên | đã chốt |
| 2026-07-13 | Bàn phím iOS câm khi test tay — 4 lớp lỗi + quyết định Full Access | Test tay trên Simulator: bàn phím không lên/không bung macro. Đào ra 4 lớp chồng: (1) ký `CODE_SIGNING_ALLOWED=NO` → App Group container KHÔNG được cấp (giải luôn câu hỏi mở dòng 2026-07-12); (2) Xcode 16+ tách `.debug.dylib` → app-extension bàn phím không nạp được; (3) `RequestsOpenAccess=false` từ R1 → iOS ẩn nút Full Access + chặn App Group; (4) kể cả vá đủ 3 cái trên, Simulator VẪN không dựng custom keyboard (pkd đã `Created plugin`, không crash — tật kinh điển máy ảo) | **ĐÃ VÁ (commit `fa0c988`):** run-ios ký ad-hoc KÈM entitlements + `ENABLE_DEBUG_DYLIB=NO`; `Info.plist RequestsOpenAccess=true`. App Group + macro + settings giờ verify được trên Simulator ở tầng dữ liệu. Phần **gõ thật bằng bàn phím = device-only** (chốt cùng nhóm Zalo/RAM). Chủ dự án đã chọn hướng **cắm iPhone thật** (Apple ID free đủ cho gõ + sóng + chuông; macro/App Group cần tài khoản trả phí) | **CẦN CHỦ DỰ ÁN PHÊ CHUẨN:** iOS giờ **xin Full Access** (open access) — chạm riêng tư (§hiến chương). Lý do bắt buộc: đọc câu cho con sóng + mở App Group cho macro/settings, TẤT CẢ on-device (đã ghi copy màn FullAccess + comment Info.plist). Owner có thể veto nếu muốn iOS bỏ macro/settings để khỏi xin quyền | mở |

| 2026-07-13 | Notification cuối ngày iOS (R3 Q5) vs mandate "nhắc thụ động" | Mandate iOS (2026-07-10) chốt "nhật ký + nhắc **THỤ ĐỘNG**, không hối thúc". Round 3 Q5 chủ dự án chọn CÓ 1 thông báo đẩy cuối ngày mời soi lại — notification = "nhắc **chủ động**", nghịch nhẹ mandate | **Đã chốt (2026-07-13):** giữ notification NHƯNG rào cứng — **mặc định TẮT, opt-in rõ ràng, tối đa 1 lần/ngày, giờ chỉnh được, tắt được, copy quan sát không phán xét** (CẤM kiểu "bạn bỏ lỡ N ngày"). Ghi ở `bmad-output/ios/decision-log.md` 2026-07-13 + `tech-spec-r3.md` story 3.4 | Đã chốt nới lỏng CÓ Ý THỨC (rào opt-in). Nếu về sau thấy phiền → veto, quay lại thuần-trong-app | đã chốt |
| 2026-07-13 | Full Access nặng ký hơn ở R3 | Dòng 2026-07-13 (Full Access) còn `mở`. R3 bắt đầu **PERSIST dữ liệu phái sinh** (send-risk sự kiện, mã hóa) thay vì chỉ sóng tức thời trong RAM → mức độ hệ trọng của việc "xin đọc mọi thứ gõ" tăng lên | Vẫn build R3 (chủ dự án yêu cầu) với đủ rào riêng tư: on-device, mã hóa AES+Keychain, không văn bản gốc, consent-gate, log tối thiểu (chỉ tense-event). KHÔNG tự đóng dòng Full Access | Chủ dự án chính thức khẳng định/đóng dòng Full Access (2026-07-13) khi đã thấy R3 persist gì | mở |
| 2026-07-13 | Giá trị `kSecAttrAccessGroup` cho khóa Keychain chia sẻ (story 3.1) | Kho nhật ký iOS cần extension (ghi) + container (đọc) dùng CHUNG 1 khóa AES qua `keychain-access-groups`. Runtime phải set `kSecAttrAccessGroup` khớp entitlement `$(AppIdentifierPrefix)vn.gnh.mindfulkey.shared`, NHƯNG `$(AppIdentifierPrefix)` chỉ resolve lúc KÝ THẬT bằng Team ID — dự án ký ad-hoc không có Team ID nên KHÔNG biết giá trị đã resolve lúc biên dịch | **Tạm xử:** hardcode `@"vn.gnh.mindfulkey.shared"` (không tiền tố) + bọc `#if TARGET_OS_IPHONE` để host test bỏ qua field này (dùng keychain mặc định, vẫn test được crypto/consent). Ghi rõ device-only ở `MoodJournalStore.mm` header + TEST_MATRIX | Khi có Apple Developer Program (Team ID thật): verify access group khớp trên máy thật; nếu `errSecMissingEntitlement` → chỉnh giá trị/tiền tố cho đúng | mở |
| 2026-07-14 | Phạm vi cột CSV export (story 2.6) | Story 2.6 tự ghi rõ đây là "DỪNG-HỎI chủ dự án" (2 cách hiểu: xuất y hệt dữ liệu đã lưu gồm `app_bundle_id`/`choice`, hay xuất hẹp hơn chỉ điểm+giờ+mood) — nhưng dev agent thi công đã TỰ CHỌN bản hẹp (comment code "Xuất hẹp... bỏ qua app_bundle_id và choice") và code chạy thẳng, không dừng lại chờ xác nhận, không ghi vào `Dev Agent Record` của story, không tạo dòng nào ở đây. Bị bỏ sót tới 2026-07-14 mới phát hiện lúc soát lỗ hổng quy trình theo dõi kế hoạch | **Đã chốt (2026-07-14, xác nhận qua hội thoại với chủ dự án):** giữ bản hẹp như code đang chạy — hẹp hơn dữ liệu lưu tại chỗ là lựa chọn ĐÚNG cho dữ liệu rời khỏi vùng mã hoá. Ghi ở `decision-log.md` entry 2026-07-14 "Chốt điểm gai #3" + cập nhật `docs/PRIVACY-NOTE.md` | Không còn — bài học rút ra: câu "DỪNG-HỎI" viết trong file story KHÔNG tự động nghĩa là có ai hỏi thật; PHẢI kèm 1 dòng ở đây thì mới nổi lên được | đã chốt |
| 2026-07-14 | Mặc định N-ngày tự xoá nhật ký (story 2.6) | Không nguồn nào chốt sẵn con số; dev agent tự đề xuất 90 ngày (`[Inference]`) và code luôn theo số đó, không dừng chờ xác nhận (mức độ nhẹ hơn dòng CSV ở trên — story tự ghi "không cần dừng hẳn") | **Đã chốt (2026-07-14, xác nhận qua hội thoại với chủ dự án):** giữ 90 ngày mặc định + 4 lựa chọn 30/60/90/Không bao giờ như code đang chạy | Không còn | đã chốt |
| 2026-07-15 | "Hôm nay" ở cửa sổ vs ở popover (nghiệm thu F2) | Có HAI chỗ tên "Hôm nay": tab trong popover (có card Gác cổng + SensitivityCard, chạy thật) và mục nav trong cửa sổ quản lý (chỉ có tiêu đề + sông rỗng). Mockup A2 vẽ card gác cổng NẰM TRONG cửa sổ. Không nguồn nào nói 2 chỗ này quan hệ thế nào — bản đầy đủ vs bản rút gọn? gương của nhau? cố ý khác vai? | **Đã chốt (2026-07-15, hỏi thẳng chủ dự án trước khi code — đúng bài học "Chốt điểm gai #3"):** "Hôm nay" ở cửa sổ = **bản ĐẦY ĐỦ** (card Gác cổng + sông + lối vào Soi lại); popover giữ bản gọn để liếc nhanh. Khớp mockup A2. Ghi ở `decision-log.md` 2026-07-15 "Chốt 3 câu chặn Epic 3" | Không còn — Epic 3 gate 3 hết chặn | đã chốt |
| 2026-07-15 | Nhịp chuông 15 phút vs trần lấy mẫu đã chốt (nghiệm thu F10) | Artifact A2 chốt nhịp lấy mẫu = **30 hoặc 60** phút → "cả ngày chỉ ~24–48 chấm". App thật có thêm mức **15** (đang chọn mặc định) → ~96 chấm/ngày, gấp đôi trần. Chuông = nhịp lấy mẫu (chốt 2026-07-13) nên đây không chỉ là chuyện âm thanh — nó là **tần suất ghi nhật ký**, tức chạm cột trụ riêng tư ("ghi nhiều hơn hiện tại") | **Đã chốt (2026-07-15):** 2 nút nhanh **30/60** + ô **"Tùy chỉnh"** điền số phút, **sàn 15 / trần 240**. Sàn tồn tại vì chuông gánh 2 vai: reo mỗi 5 phút = **hối thúc** (HIẾN CHƯƠNG cấm), và nhịp dày = nhật ký thành **dòng thời gian cảm xúc chi tiết**, phá trần riêng tư. Sàn = 15 vì đúng bằng mức thấp nhất đang chạy. Ghi ở `decision-log.md` 2026-07-15 | **Còn 1 việc kéo theo:** `docs/PRIVACY-NOTE.md` đang hứa "~24–48 chấm/ngày" — chỉ đúng cho 30/60. Đặt 15 → ~96 chấm. Phải sửa thành khoảng thật | đã chốt *(còn việc kéo theo)* |
| 2026-07-16 | `make build` và `make run` xuất ra HAI nơi khác nhau — bẫy "vá rồi mà vẫn sập" | `make build` chạy `xcodebuild` KHÔNG kèm `-derivedDataPath` → sản phẩm rơi vào `~/Library/Developer/Xcode/DerivedData/MindfulKey-<hash>/`. `make run` thì CÓ kèm `-derivedDataPath platforms/apple/build` → sản phẩm rơi vào trong repo. Hệ quả: (a) `make run` build **2 lần** (vì `run: build`), ra 2 bản ở 2 nơi; (b) ai `make build` rồi `open platforms/apple/build/...` sẽ chạy **bản CŨ**, tưởng đã vá mà thật ra chưa. **Đã cắn thật 2026-07-16:** agent vá crash `MKRiverCanvas` lúc 00:59 (vào DerivedData), rồi mở nhầm bản 00:37 ở `platforms/apple/build`, tuyên bố "đã vá, chạy ổn định" — app vẫn sập lúc 01:02 đúng lúc chủ dự án thử mở. Mất ~30 phút + 1 lần báo sai cho chủ dự án | **Đã chốt + đã vá (2026-07-16, cùng phiên):** chọn phương án (1) — thống nhất mọi thứ về `$(DERIVED)`. `build` nay kèm `-derivedDataPath "$(DERIVED)"` (nên `make clean` dọn được thật); `run` bỏ lần build thứ 2, đổi thành **build → cài `/Applications` → mở**, để bản đem test LUÔN là bản Spotlight/Finder mở; thêm `make doctor` liệt kê bản lạc + bản đang chạy. `scripts/build-dmg.sh` đã học layout mới (`build/Build/Products/<config>/`), nhánh dò DerivedData hạ xuống cuối + ghi rõ là dự phòng cho bản build bằng Xcode GUI. CI không đụng (nó gọi `xcodebuild` thẳng, không qua `make build` — đã kiểm) | Không còn | đã chốt |
| 2026-07-15 | Tên sản phẩm hiển thị (nghiệm thu F9) | Tiêu đề cửa sổ thật: **"Cài đặt Mindful Keyboard"**. Nhưng artifact A2 + repo (`mindful-key`) + DMG (`MindfulKey-0.2.0.dmg`) + wordmark brand đều gọi **"mindful-key"**. Repo cha thì tên `mindful-keyboard`. Ba cách gọi cùng tồn tại, không nguồn nào chốt cái nào là tên hiển thị cho người dùng | **Đã chốt (2026-07-15):** tên hiển thị cho người dùng = **"Mindful Key"** (2 chữ, viết hoa). Khớp wordmark + repo + DMG. Tiêu đề cửa sổ "Cài đặt Mindful Keyboard" → đổi theo. Ghi ở `decision-log.md` 2026-07-15 | **Còn 1 việc kéo theo:** viết luật tên vào `docs/BRAND-ASSETS.md` + quét chữ "Mindful Keyboard" còn sót trong UI | đã chốt *(còn việc kéo theo)* |
| 2026-07-16 | Dòng sông đọc thành mấy khúc sóng rời (chủ dự án phản hồi "chưa hiểu màn Hôm nay") | Sông chỉ vẽ nước+chấm ở chỗ có mẫu (đúng dec.4 "không bịa nước giả"), nên sáng/chiều thành 2 khúc trôi rời, giữa trống → không đọc thành MỘT dòng. Muốn nối cả ngày nhưng KHÔNG được reverse dec.4 (vẽ nước phẳng = nói dối "phẳng lặng") | **Đã làm (2026-07-16), chủ dự án đã duyệt qua mockup trước khi code:** thêm **trục thời-gian nét đứt mờ (Brand stone, alpha 0.5)** chạy suốt ngày ở `EmotionRiverView.mm` — là TRỤC (cùng họ nhãn Sáng/Trưa/Chiều/Tối), KHÔNG phải mặt nước; nước teal đặc + chấm vẫn CHỈ ở mẫu thật → dec.4 nguyên vẹn. Kèm vá bug chấm tô TRẮNG (tàng hình trên thẻ trắng) → tô teal đặc 6.6px cho "vòng tròn" thật hiện. Chốt trục dọc = **chỉ cường độ, không valence**. brand-lint 0 · make build sạch · make test xanh | Chủ dự án có thể veto trục nét đứt nếu thấy vẫn giống "phẳng lặng giả"; và chọn có làm tab Tuần/Tháng cùng ngôn ngữ này không | đã chốt |
| 2026-07-16 | Bề rộng thẻ + eyebrow pane "Bộ gõ ▸ Kiểu gõ" (batch 2 UI redesign, `SCREEN-REFERENCE.md` §2.3) | §2.3 chỉ nói "1 thẻ chứa lưới PillSwitch (2 cột)" — không nói rộng bao nhiêu hay có eyebrow hay không. Thẻ hiện tại (thừa kế từ storyboard OpenKey cũ, box `HsQ-jf-eR7`) rộng 520pt, HẸP hơn `kMaxPaneW`=600pt mà mọi pane brand khác (Chuông/Riêng tư/Hệ thống) lấp đủ — để hở ~80pt trắng bên phải. Và §2.2/§2.4 đều liệt kê eyebrow tên riêng cho từng thẻ, còn §2.3 thì không nhắc | **Chọn giữ nguyên, KHÔNG tự chế:** không nới thẻ lên 600pt — muốn vậy phải tính lại toạ độ x của cả lưới 2 cột (11 switch + 11 nhãn, số point cụ thể trong storyboard), rủi ro lệch/đè chữ cao hơn lợi ích thẩm mỹ, và spec không bắt buộc. Không thêm eyebrow — nút subnav "Kiểu gõ" vừa bấm đã đóng vai trò tên nhóm, thêm "KIỂU GÕ" ngay dưới sẽ lặp chữ (khác Chuông/Riêng tư vốn nhiều nhóm trong 1 mục nav nên cần eyebrow phân biệt) | Chủ dự án xác nhận: (a) có cần nới thẻ full-width 600pt cho khớp Chuông/Riêng tư/Hệ thống không; (b) có cần thêm eyebrow phía trên thẻ Kiểu gõ không — và nếu có thì tên gì (không có sẵn trong tài liệu nào) | mở |

| 2026-07-16 | Hợp đồng vòng đời của `vOnWordCommitted` chỉ được ghi TRONG vỏ iOS | `Engine.h:224` khai báo `void (*vOnWordCommitted)(const wstring& word)` — không một chữ nào nói tham chiếu đó trỏ tới **biến cục bộ** `w` của `emitCommittedWord()` (`Engine.cpp:463`), chết ngay khi callback return. Vỏ iOS đã cắn phải rồi tự vá + ghi bài học vào comment `MoodBridge.mm:41-45` (triệu chứng của họ: `recentText()` rỗng). Kiến thức đó **nằm trong file của đội iOS**, không có ở header engine, không có ở skill/CLAUDE.md → vỏ macOS ngã đúng hố đó và **không ai biết suốt nhiều tháng**: `MoodWatchMac.mm` capture thẳng `word` vào block `dispatch_async`. Triệu chứng bên macOS nặng hơn iOS: lúc mất chữ (lexicon phân tích thiếu từ vừa gõ), lúc `abort()` (**10 crash đêm 2026-07-16, cái cuối 01:44** — backtrace: `pushWord` → `push_back` → copy ctor → `length_error`) | **Đã vá (2026-07-16):** `MoodWatchMac.mm` copy `wstring wordCopy = word;` TRƯỚC `dispatch_async`, đặt trùng tên biến với `MoodBridge.mm` để grep `wordCopy` ra cả 2 vỏ. Cơ chế được **chứng minh bằng test thật, không suy từ backtrace**: trong block, `&word` vẫn đúng ô nhớ đã chết (size 24 → 0); bản chưa vá ghi **0 từ**, bản vá ghi **1 từ**. `core/` KHÔNG đụng (nó nhận `const&` rồi tự copy — hoàn toàn đúng; lỗi 100% ở vỏ) | **Nên viết luật ở tầng engine, đừng để mỗi vỏ tự cắn 1 lần:** (a) ghi thẳng vào `Engine.h` cạnh khai báo `vOnWordCommitted` rằng tham chiếu **chỉ sống trong lúc gọi** — vỏ nào muốn dùng bất đồng bộ PHẢI tự copy; (b) vỏ Windows/Linux sau này sẽ gặp y hệt — có cần dòng này trong skill `platform-porting` không? Sửa `Engine.h` = chạm `core/` đang đóng băng với đội iOS → cần chủ dự án chốt | mở |

| 2026-07-16 | Cô lập kho cho test host macOS — env `HOME` KHÔNG ăn | Viết test E2E `tests/macos/mood_pipeline` cần cô lập `mood.enc` khỏi kho thật. Cách hiển nhiên (tests/ios từng dùng tư duy tương tự): set env `HOME` = thư mục tạm. **Verify thực nghiệm: KHÔNG ăn** — `NSHomeDirectory()`/`URLForDirectory:` trên macOS lấy home qua `getpwuid`, phớt lờ `$HOME` (in ra `/Users/now` dù `getenv("HOME")` = thư mục tạm). Không tài liệu nào trong repo cảnh báo | **Đã xử (2026-07-16):** thêm nhánh test-only vào `SupportDirectoryURL()` (MoodStoreMac.mm) đọc env `MK_TEST_STORE_DIR`, bọc `#ifdef MK_TEST_STORE_DIR_ENV` — CHỈ script test định nghĩa macro, binary app thật không có nhánh này. Keychain thì cô lập bằng `-D` rename 3 hàm `SecItem*` sang stub (khỏi đụng khóa thật + khỏi hộp thoại xin quyền treo CI). Test tự abort nếu env không trỏ thư mục tạm | Không còn — bài học ghi ở `tests/macos/README.md` cho test host sau này | đã chốt |
| 2026-07-16 | File nháp của phiên song song rơi vào `platforms/apple/macos/` làm gãy `make build` | Trong lúc phiên này chạy test E2E (20:45–20:46), một phiên khác thả `test_enabled.m` + `test_hit.m` (mỗi file 1 `main()` nháp probe NSControl) + `a.out` vào THẲNG thư mục nguồn vỏ macOS. XcodeGen glob `*.m` → 3 symbol `_main` trùng → `make build` FAIL toàn cục. Phiên này KHÔNG xóa (file của đội khác, quy ước 2 đội chung tree) — nhưng ai chạy preflight lúc đó sẽ tưởng lỗi của mình | Bản vá MoodStoreMac.mm của phiên này được chứng minh biên dịch+link sạch qua chính binary test E2E (không cần full build). Báo chủ dự án trong phiên | Chủ dự án: (a) dọn/di dời 3 file nháp (`test_enabled.m`, `test_hit.m`, `a.out`); (b) có nên thêm luật "file nháp/probe CẤM để trong `platforms/` — dùng scratchpad" vào CLAUDE.md không? | mở |
| 2026-07-16 | Windows gói móng — `MoodWatch.cpp`/`Bell.cpp` không nằm trong `OpenKey.vcxproj` nhưng vẫn bị gọi | Rà soát build-readiness Windows phát hiện: `MainControlDialog.cpp`/`OpenKey.cpp`/`SystemTrayHelper.cpp` include `MoodWatch.h`/`Bell.h` và gọi `vMoodWatch`/`MoodWatch_Init()`/`MoodWatch_Toggle()`/`Bell_Init()`/`Bell_ShowSettings()` — nhưng `MoodWatch.cpp`/`Bell.cpp` (định nghĩa các hàm/biến đó) KHÔNG có trong `vcxproj` (`ClCompile`). Dự đoán chắc chắn LNK2019 khi build thật trên Windows. Nhiệm vụ gói móng ghi rõ "KHÔNG port lớp cảm xúc" nên KHÔNG tự quyết cách vá (2 hướng khả dĩ: (a) thêm `MoodWatch.cpp`/`Bell.cpp` vào `vcxproj` — nhưng vậy là wire cả lớp mood-cũ vào build sớm, ngược ý "để gói sau"; (b) comment tạm 5 điểm gọi ở 3 file vỏ — surgical hơn nhưng đụng logic UI (checkbox MoodWatch, khởi tạo Bell, menu tray) ngoài phạm vi "chỉ build-readiness + rebrand chuỗi" được giao) | **Đã chốt hướng (a) + đã vá (2026-07-16):** chủ dự án ra brief "đưa Windows ngang macOS + đóng gói .exe", ghi rõ "gỡ LNK2019 là việc đầu tiên" (Mốc 1) và "port lớp cảm xúc từng module" (Mốc 2) → chọn wire `MoodWatch.cpp`/`Bell.cpp` + 2 header vào `ClCompile`/`ClInclude` của `OpenKey.vcxproj` (+ `.filters`). Chỉ đụng 2 file cấu hình project, KHÔNG đụng dòng code nào, KHÔNG đụng `core/`. Đã kiểm trước: XML hợp lệ, PCH đặt `NotUsing` (không bẫy C1010), cả 2 `.cpp` đã có `#include "stdafx.h"`, `IDD_DIALOG_BELL`+`IDC_*` đủ trong `resource.h`/`.rc`. **CHƯA build-verify** — máy dev là macOS, chờ chủ dự án chạy VS2022 (project đòi toolset v143) | **Sinh ra 2 khoản nợ mới, hẹn trả ở Mốc 2 (port MoodWatch):** (1) `MoodWatch.cpp:214-219` tự gom câu bằng mảng `g_words` riêng thay vì gọi `core/mood/MoodBuffer` → phạm ranh giới cứng "vỏ không chép logic bộ não"; (2) `MoodWatch.cpp:177-181` hiện `MessageBox` **dán nhãn** cảm xúc "GIẬN/BUỒN/MỆT/LO" viết hoa → lệch HIẾN CHƯƠNG §2.2 (nhận diện = sóng `~` theo biên độ, trung tính, không phân loại); bản macOS đã bỏ kiểu này. **CẦN CHỦ DỰ ÁN CHỐT nếu Mốc 3 (installer) chạy TRƯỚC Mốc 2:** `vMoodWatch = 1` (bật mặc định) → bản `.exe` phát hành sẽ bật sẵn popup dán nhãn lệch hiến chương. Giữ nguyên, hay đổi mặc định thành TẮT cho tới khi MoodWatch được port xong? | đã chốt *(còn nợ Mốc 2)* |
| 2026-07-16 | Windows gói móng — CompanyName VERSIONINFO chưa có tên chính thức | `.rc` VERSIONINFO có `VALUE "CompanyName", "The OpenKey Project"` — pháp lý/brand field hiện trong Properties file Windows. Không tìm thấy tên công ty/tổ chức chính thức nào cho mindful-key trong `brand/tokens.json`/`docs/BRAND-ASSETS.md` (chỉ có "GNH — Lan tỏa điều tử tế" như tên chương trình, không rõ có phải pháp nhân hay không) | **Giữ nguyên "The OpenKey Project"** — không tự bịa tên công ty cho 1 field pháp lý | Chủ dự án chốt CompanyName nên là gì: "GNH", để trống, giữ nguyên, hay tên khác | mở |
| 2026-07-16 | Windows gói móng — link Facebook "OpenKeyVN" trong About/Info box | 2 SysLink trong `.rc` (About box + tab Thông tin) trỏ `https://www.facebook.com/OpenKeyVN` — fanpage của dự án OpenKey GỐC (Mai Vũ Tuyên), không phải kênh của mindful-key. Không rõ nên: giữ nguyên (thuần credit, giống dòng "Mai Vũ Tuyên © 2019"/email tác giả), xóa hẳn control (macOS AboutViewController không có mục fanpage tương đương), hay đổi sang fanpage riêng của mindful-key (chưa thấy định nghĩa ở đâu) | **Giữ nguyên URL** — coi là credit tới dự án gốc, không đổi/xóa vì mơ hồ pháp lý/brand | Chủ dự án chốt: giữ/xóa/đổi link fanpage OpenKeyVN trong 2 dialog Windows | mở |
| 2026-07-16 | Windows gói móng — `InternalName`/`OriginalFilename` VERSIONINFO vẫn ghi "OpenKey.exe" | `.rc` có `VALUE "InternalName", "OpenKey.exe"` + `VALUE "OriginalFilename", "OpenKey.exe"` — chứa chữ "OpenKey" nhưng đây là field mô tả TÊN FILE THẬT sẽ được build ra (vẫn là `OpenKey.exe` vì KHÔNG đổi tên project/target theo lằn ranh cứng của nhiệm vụ) | **ĐÃ CHỐT + ĐÃ LÀM (2026-07-17):** chủ dự án chọn **đổi tên file thật thành `MindfulKey.exe`**, tức chủ động NỚI lằn ranh "KHÔNG đổi tên file" mình tự đặt lúc giao việc. `TargetName` nay đặt `MindfulKey` cho **cả 4 cấu hình** (Debug trước đây không đặt nên ra `OpenKey.exe`, Release ra `OpenKey64.exe`); `InternalName`/`OriginalFilename` khớp theo. Tên **project/solution/class vẫn giữ OpenKey** — chỉ tên file xuất ra đổi. **Bẫy suýt lọt:** `OpenKeyHelper.cpp:139` so tên file của CHÍNH MÌNH với `"OpenKey64.exe"` để biết "cửa sổ trước mặt là mình, đừng coi là đổi app" — quên sửa là nhánh đó không bao giờ khớp, SmartSwitchKey nhớ nhầm chế độ ngôn ngữ cho chính bộ gõ, mà **build vẫn sạch** nên CI không bắt được. Đã sửa. | **Nợ mới, chưa làm — chủ dự án quyết:** (a) `OpenKeyUpdate.cpp` còn hardcode `OpenKey64.exe` 3 chỗ (xoá/thay file lúc cập nhật) → bộ cập nhật nay trỏ vào hư không. Hiện KHÔNG build/ship nó nên không hại bản phát hành; (b) nút **"Kiểm tra bản mới..."** trong hộp Giới thiệu chạy `OpenKeyUpdate.exe` — bộ cài KHÔNG kèm file này, nên nút đó sẽ **hỏng ngay từ bản đầu**. Cần chốt: bỏ nút, hay ship updater, hay dùng cơ chế khác | đã chốt *(còn 2 nợ)* |

| 2026-07-17 | 3 icon `bell_temple`/`bell_chime`/`bell_wind` có SVG nguồn nhưng KHÔNG do `brand/export.sh` sinh ra | Thêm ô thứ 4 "tiếng của bạn" cần 1 hình. `docs/BRAND-ASSETS.md` chốt luật "1 nguồn SVG → xuất qua `brand/export.sh`", nhưng soi thật thì `export.sh` **không hề** sinh 3 icon chuông đang dùng: `brand/svg/bell_temple.svg`/`bell_chime.svg`/`bell_wind.svg` có tồn tại, mà file png/@2x trong `platforms/apple/macos/` thì ai đó xuất TAY (export.sh chỉ nhắc tới `bell-idle`/`bell-ring`, và chỉ xuất chúng vào `brand/png/` để xem trước). Tức 3 icon nhận diện đang sống ngoài dây chuyền — sửa SVG nguồn thì app không đổi theo, không ai biết | Không đoán, không tự động "sửa cho gọn": chỉ **thêm** `bell-idle` (48/96) vào `export.sh` cho đúng luật với ô mới, và ghi rõ lệch pha ngay tại chỗ trong `export.sh` + dòng này. Ba icon cũ giữ nguyên, không đụng | Chủ dự án chốt: có kéo `bell_temple`/`bell_chime`/`bell_wind` vào `export.sh` (rồi xuất lại, có thể lệch pixel so với bản xuất tay hiện tại) hay cố ý để ngoài dây chuyền? | mở |
| 2026-07-17 | Tên file 3 `.wav` vẫn tiếng Việt có dấu — nghịch luật "định danh = tiếng Anh" | Đổi kho lưu bộ tiếng sang id tiếng Anh (`temple`/`chime`/`wind`) đã gỡ được phần nghịch luật ở **giá trị UserDefaults**. Nhưng chính tên FILE — `Chuông chùa.wav`/`Chuông gió.wav`/`Chuông reo.wav` — vẫn là chữ có dấu, mà CLAUDE.md xếp *file name* vào nhóm "máy đọc ⇒ luôn tiếng Anh". Đổi tên file thì phải đụng `project.pbxproj` + bundle + đóng gói — đúng vùng vừa mới có bug người dùng thật báo (chuông không kêu) | **Không đổi tên file trong lượt này** (nguyên tắc mổ ruột thừa đừng sửa mũi + tránh đụng đóng gói ngay sau sự cố đóng gói). Cô lập thiệt hại: gom tên file về **đúng 1 hàm** `ResourceNameForSoundId()` trong `BellMac.mm` — mọi nơi khác trong app chỉ nói bằng id tiếng Anh | Chủ dự án chốt: có đổi tên 3 file .wav sang tiếng Anh (`bell-temple.wav`…) thành 1 việc riêng không? Rẻ, nhưng chạm bundle nên phải build + nghe lại | mở |
| 2026-07-17 | `BellMac_InstallCustomSound` chưa có test vì kho ghi thẳng vào Application Support thật | Ca 8 (`tests/macos`) khoá được `BellMac_SoundIdFromStored` (hàm thuần, verify RED thật). Nhưng phần chép tệp + dội cache thì chưa: `CustomSoundDirURL()` trỏ `~/Library/Application Support/MindfulKeyboard/` THẬT — chạy test sẽ ghi/xoá `CustomBell.*` trong thư mục thật của chủ dự án. `MoodStoreMac` giải bằng nhánh `#ifdef MK_TEST_STORE_DIR_ENV`, nhưng chép y hệt nhánh đó sang `BellMac.mm` = **2 nơi cùng tính "thư mục của app"** — đúng loại trùng lặp vừa đẻ ra con bug hôm nay (2 nơi cùng đoán mặc định) | Không chép nhánh test sang BellMac. Để trống test cho phần install, ghi rõ ở `TEST_MATRIX.md` thay vì giả vờ đã phủ | Chủ dự án chốt: có tách 1 hàm dùng chung "thư mục dữ liệu app" (cho cả MoodStore lẫn BellMac, kèm 1 nhánh test duy nhất) thành việc riêng không? Làm xong thì test được cả luồng chuông riêng | mở |
| 2026-07-17 | brand-lint không phân biệt "ta CẤM X" với "ta LÀM X" | Cổng nhận diện chặn theo TỪ KHOÁ (`\b(streak\|badge\|combo\|...)\b`, `huy hiệu`, ...). Nó bắt cả từ nằm trong câu PHỦ ĐỊNH chính nó. Cắn 3 lần trong 1 phiên: (1) biến `combo` = ComboBox của Win32, thuật ngữ chuẩn Windows, không liên quan gamification; (2) comment giải thích cách né lần (1) lại chứa chính từ đó; (3) comment `// cố ý KHÔNG gamify: không điểm, không streak, không huy hiệu` — câu KHẲNG ĐỊNH ràng buộc hiến chương lại bị chặn vì liệt kê ra thứ bị cấm | **Né thủ công:** đổi biến `combo`→`hCombo` (regex `\bcombo\b` không bắt), và bỏ hẳn phần liệt kê trong comment, chỉ trỏ tới HIẾN CHƯƠNG §2.2. Hoạt động, nhưng là chơi trò đuổi bắt — **GĐ6 (vỏ nhận diện) sẽ có RẤT nhiều ComboBox và rất nhiều comment nói về điều bị cấm** | Chủ dự án chốt: (a) để nguyên, người viết tự né (rẻ, nhưng dạy agent viết comment né luật thay vì nói thật); (b) thêm lối thoát có kiểm toán kiểu `// brand-lint: ok <lý do>` — nhưng đó là lỗ hổng trong một ràng buộc CỨNG, có thể bị lạm dụng; (c) tinh chỉnh regex bỏ qua ngữ cảnh phủ định (`KHÔNG`/`không`/`cấm` đứng gần) — mong manh, dễ bỏ lọt thật | mở |
| 2026-07-17 | Icon khay XÁM của Windows — brand chưa định nghĩa | Vỏ Windows có tuỳ chọn `vUseGrayIcon` (SystemTrayHelper.cpp:288) đổi sang icon khay đơn sắc. macOS làm việc tương đương bằng `setTemplate:YES` — tính năng RIÊNG của macOS: hệ thống tự chuyển ảnh thành đơn sắc thích ứng thanh menu sáng/tối. **Windows không có gì tương đương**, khay cần pixel thật, nên phải CHỌN một màu cụ thể. Brand không có token nào tên "gray tray icon". (Tên file `*10.ico` là di sản OpenKey và nói dối: không phải "kiểu Windows 10" mà là bản xám) | **Chọn token `stone` (#8A9BA0)** — "sắc độ trung tính-đá" theo `tokens.json`, đọc được trên cả khay sáng lẫn tối. Đây là CÁCH ĐỌC brand, không phải bịa màu mới; đảo lại rẻ (đổi 1 biến trong `brand/export-platform.sh` rồi `make brand-platform`). Đã verify màu THẬT trong .ico bằng Pillow: Viet=#1D7C91, Viet10=#8A9BA0, Eng10=#8A9BA0 | Chủ dự án xác nhận `stone` là màu đúng cho icon khay xám trên Windows, hay muốn màu/hình khác | mở |
| 2026-07-17 | `DefaultButton.ico` ("Cài đặt gốc") — brand không có hình | 8/9 icon Windows nay sinh từ SVG nguồn brand. Riêng nút "Cài đặt gốc" không có SVG tương ứng trong `brand/svg/` (có OK, ExitButton, StartConvert, Status, StatusEng... nhưng không có "khôi phục mặc định") | **Giữ icon OpenKey gốc** cho riêng nút đó — bịa một hình là tự chế nhận diện. Nên trong thư mục Windows còn ĐÚNG 1 file .ico đời cũ | Chủ dự án: vẽ thêm 1 SVG cho hành động "Cài đặt gốc", hay bỏ icon khỏi nút đó (chỉ để chữ)? | mở |
| 2026-07-17 | `StatusEng.svg` dùng màu #9AA0A6 — KHÔNG có trong tokens.json | Phát hiện khi đọc màu thật của icon vừa sinh: `StatusEng.svg` (icon khay chế độ tiếng Anh) stroke `#9AA0A6`. `brand/tokens.json` không có màu này (gần nhất: `stone` #8A9BA0, `muted` #666666). Tức tài sản brand NGUỒN đang hardcode một màu ngoài bảng token — đúng thứ `brand-lint` chặn ở code, nhưng `walk_ui()` chỉ quét `platforms/` + `site/` nên `brand/svg/` nằm ngoài tầm | **KHÔNG tự sửa** — đây là tài sản brand có sẵn từ trước, đổi màu icon là chạm nhận diện. Chỉ ghi nhận | **ĐÃ CHỐT + ĐÃ LÀM (2026-07-17):** brand-lint **nay quét `brand/`** (167→214 file) và coi mọi thứ trong đó là **brand-critical theo ĐỊNH NGHĨA**. Không sinh nhiễu: `not is_svg` (brand_lint.py) đã miễn .svg khỏi kiểm palette từ trước, nên màu truyền thông/wordmark/icon-nền-tảng không bị báo bừa. **BÀI HỌC ĐẮT:** vòng đầu tôi chỉ thêm `brand` vào `walk_ui()` rồi **viết vào commit message rằng "verified by attack: #FF0000 bị bắt"** — SAI. Nó chỉ **cảnh báo**, exit 0, CI vẫn xanh, vì `BRAND_CRITICAL` xét theo TÊN FILE và `Status.svg` không khớp tên nào. Tức con sóng `~` — dấu ấn của cả thương hiệu — chuyển đỏ mà vẫn qua cổng. Đã vá + đo lại: nay exit≠0. **Tôi grep `❌` rồi thấy rỗng và kết luận "không vi phạm", trong khi nó đang in `⚠️`** — công cụ đo sai còn nguy hơn không đo. | **CÒN 1 câu:** `#9AA0A6` là màu DUY NHẤT vừa ship vào UI (icon khay chế độ Anh, cả macOS lẫn Windows) vừa không có tên trong tokens.json. BRAND-ASSETS §4 mô tả nó có chủ đích ("nét thẳng xám — nín thở"). Chủ dự án chốt: đặt tên nó thành token (vd `engGray`), hay đổi về `stone`? KHÔNG tự làm — đặt tên/đổi màu brand là quyết định nhận diện | đã chốt *(còn 1 câu)* |
| 2026-07-17 | "lắng về Status sau VÀI GIÂY" — bao nhiêu giây? | `BRAND-ASSETS.md §6` chốt hành vi StatusAlert: đổi icon khay khi mức 4-5 "rồi lắng về `Status` sau **vài giây**". "Vài giây" không phải con số máy hiểu được, và không nguồn nào khác chốt | **Dùng 3s, neo vào mỏ neo CÓ SẴN của chính sản phẩm:** `BreathingPausePrompt::durationSeconds` mặc định 3.0 (`core/mood/BreathingPause.h`) — tức dự án đã một lần định nghĩa "một nhịp đủ để người ta kịp nhận ra" là 3 giây. Không bịa số mới. Ghi hằng `kWaveAlertMs` ở SystemTrayHelper.cpp kèm lý do | Chủ dự án xác nhận 3s, hay muốn khác. Cũng nên chốt: gợn tiếp trong lúc icon đang động thì GIA HẠN 3s (đang làm vậy) hay để nó lắng đúng hạn? | mở |
| 2026-07-17 | Mức 4-5 của thang mặt hồ ứng với risk bao nhiêu | `BRAND-ASSETS §6` nói StatusAlert bật ở "mức 4-5", nhưng KHÔNG có code nào map risk 0..1 -> mức 1..5. `MoodStoreMac_LogCheckinEvent(waveLevel)` nhận mức từ NGƯỜI DÙNG tự chọn (check-in), không suy từ risk | **Suy từ mỏ neo ngữ nghĩa của §5, không bịa ngưỡng:** mức 4 "Sóng" = "chuông có thể ngân mời" -> `NudgeCoordinator_RippleThreshold()`; mức 5 "Cuộn" = "kích hoạt lớp nhịp thở" -> ngưỡng gác cổng. Nên mức 4-5 bắt đầu ở ngưỡng gợn, và tự tôn trọng Độ nhạy người dùng chọn. Kết hợp `EmotionWaveAmplitude()` (dead-zone 0.3) để mặt hồ phẳng tuyệt đối dưới ngưỡng | Chủ dự án: có muốn một phép map risk->mức 1..5 TƯỜNG MINH trong `core/mood` không (macOS/iOS rồi cũng sẽ cần khi vẽ thang mặt hồ), hay cứ để mỗi chỗ tự neo theo ngữ nghĩa? | mở |
| 2026-07-17 | **CHẶN PHÁT HÀNH** — Windows đọc ô mật khẩu, macOS/iOS thì không | Điều tra khi chủ dự án yêu cầu tạo release. **iOS** chặn bằng CODE (`MoodBridge_SetSecureFieldActive`, gọi trước khi engine chốt từ). **macOS** KHÔNG có dòng code nào — nó được HỆ ĐIỀU HÀNH che: Secure Input Mode chặn CGEventTap ở ô mật khẩu. **Windows KHÔNG có gì**: `WH_KEYBOARD_LL` thấy hết, và Windows không có cơ chế tương đương. -> Windows là vỏ DUY NHẤT mà mật khẩu vào MoodBuffer, bị chấm risk, có thể bật lời nhắc giữa lúc đăng nhập, và nhịp đó vào nhật ký. Chạm CỘT TRỤ RIÊNG TƯ. **Tôi từng viết sai trong QA-WINDOWS.md rằng "bản macOS/iOS có" — khẳng định mà không kiểm; đã sửa** | **Chủ dự án chốt phương án (1) — vá bằng UIA. ĐÃ CODE (2026-07-17, cùng ngày):** file mới `platforms/windows/win32/OpenKey/OpenKey/SecureField.{h,cpp}` — luồng riêng (KHÔNG phải luồng hook) dựng `SetWinEventHook(EVENT_OBJECT_FOCUS)` + COM apartment riêng; mỗi lần focus đổi hạ cờ "che" NGAY (fail-closed) rồi mới hỏi (a) lớp rẻ `GetGUIThreadInfo`+`ES_PASSWORD` cho control Win32 gốc, (b) UI Automation `UIA_IsPasswordPropertyId` cho control tự vẽ (Chrome/Electron/UWP) nếu (a) không khớp. `MoodWatch_OnWord` (bỏ từ + xoá `MoodBuffer`) và `SendGatekeeper_ShouldIntercept` chỉ ĐỌC 1 cờ có sẵn trên luồng hook, không gọi UIA tại chỗ. `git diff core/` rỗng — sửa đúng ở vỏ. `vMoodWatch` **VẪN giữ TẮT mặc định** (không tự ý bật) — copy `MoodWatch_ConfirmEnable` đã sửa lại cho khớp đúng thứ đang chạy, không còn nói "chưa phân biệt được ô mật khẩu" nữa (điều đó không còn đúng) | **CÒN 2 việc, không tự làm được vì máy dev là macOS:** (1) mắt người trên Windows thật xác nhận đủ `docs/QA-WINDOWS.md` §5 ca P1a–P1f (đặc biệt P1e — màn đăng nhập Windows, giá cao nhất; P1b–P1d là ca UIA phủ Chrome/UWP/Electron, MÁY DEV KHÔNG KIỂM ĐƯỢC) — CI Windows chỉ chứng minh compile+link, không chứng minh cờ hạ đúng lúc; (2) sau khi có (1) PASS, chủ dự án quyết có bật `vMoodWatch` mặc định hay vẫn giữ opt-in | đã chốt *(còn chờ mắt người + quyết định bật mặc định)* |
| 2026-07-17 | Chủ dự án chốt: thấy OpenKey là TỰ TẮT, không hỏi (ghi đè hộp thoại 2 nút cùng ngày) | Bản vá P0 buổi sáng thay cú terminate câm bằng NSAlert 2 nút. Chủ dự án chốt lại ngay sau đó: "mở MindfulKey trực tiếp tắt luôn OpenKey để không bị đụng, đảm bảo ổn định" — ưu tiên mạch chạy thẳng, không bắt người dùng bấm | **ĐÃ LÀM (AppDelegate.m):** tắt theo bậc thang terminate → 1.5s sau forceTerminate → 1.5s nữa vẫn sống (gần như không thể) mới hiện hộp thoại 1 nút rồi tự thoát an toàn. Kèm notification mô tả (giọng quan sát §2.2, best-effort — lần chạy đầu quyền notification có thể chưa kịp cấp). KHÔNG modal, KHÔNG chặn launch | **2 chỗ tôi suy diễn, cần chủ dự án xác nhận:** (a) watcher NSWorkspaceDidLaunchApplicationNotification — OpenKey mở SAU (vd Login Items bật lại lúc khởi động máy) cũng bị tắt; suy từ mandate "đảm bảo ổn định", nhưng nghĩa là người dùng CỐ Ý mở OpenKey khi MindfulKey đang chạy cũng bị tắt — đúng ý không?; (b) có mở rộng cho EVKey/UniKey/GoTiengViet không — CHƯA làm vì chưa ai xác minh bundle id thật, luật cấm bịa | mở *(code xong, chờ xác nhận 2 điểm + mắt người test)* |
| 2026-07-17 | Kênh cài macOS né "damaged" không cần $99 — cơ chế quarantine | Người dùng thật dính "damaged" (tải 0.2.1 qua Chrome). Chủ dự án hỏi có tip nào không cần Apple Developer $99. Điều tra: cờ quarantine do TRÌNH DUYỆT dán; curl không dán → script cài qua Terminal thoát cảnh "damaged" một cách hợp lệ (cùng cơ chế Homebrew). Tài khoản Xcode miễn phí KHÔNG notarize được — chỉ ký chạy máy mình, 7 ngày | **ĐÃ LÀM:** `scripts/install-macos.sh` (test thật: cài từ dmg local, idempotent, 404 thoát sạch) + mục "Cài nhanh qua Terminal" trong `site/index.html` + `docs/INSTALL-MACOS-BETA.md` nói thẳng 3 đường và giới hạn từng đường. Link `releases/latest/download/MindfulKey.dmg` đo thật đang **404** (job release chưa chạy lần nào) — script báo lỗi tử tế, link tự sống từ tag tới | Chủ dự án: (a) xác nhận đây là kênh beta chính thức tới khi có $99; (b) $99 + notarize vẫn là việc PHẢI làm trước bản public rộng — chốt thời điểm | mở |
| 2026-07-18 | Bộ cài Windows CHẶN KHỞI ĐỘNG — người dùng thật báo "cài xong không mở, không xoá được" | Người dùng thật trên máy Windows: cài `MindfulKey-setup.exe` xong không có gì mở ra, và không xoá được file setup ("used by another process"). Hai triệu chứng là MỘT bug: `OpenKeyInit()` gọi `MoodStore_AskConsentIfNeeded()` (MessageBoxW modal) — mà `OpenKeyInit()` chạy ở `AppDelegate::run()` dòng ~98, TRƯỚC `createSystemTrayIcon()` (~101) và TRƯỚC vòng lặp thông điệp (~115). Modal chủ NULL treo cả hàm → không có icon khay, không có pump; hộp thoại vừa do bộ cài sinh nên không có quyền foreground → nằm sau cửa sổ khác, vô hình. Tiến trình sống mà giữ handle → bộ cài không xoá nổi. **macOS làm ĐÚNG** (`AppDelegate.m:253` hỏi trong `dispatch_async(main_queue)` = sau khi UI dựng xong); port sang Windows đánh rơi chữ "lát nữa" | **ĐÃ VÁ (2026-07-18, commit `e68f101`):** bỏ consent khỏi `OpenKeyInit()`; chuyển sang hỏi lúc người dùng CHỦ ĐỘNG bật (`MoodWatch_Toggle` khi bật, `SendGatekeeper_ToggleLastApp` khi thêm app canh). Lớp cảm xúc ship TẮT + allow-list rỗng nên hỏi lúc khởi động là xin phép cho việc chưa thể xảy ra. Giữ nguyên tinh thần "không hỏi giữa lúc căng" — lúc tự tay bật là lúc bình tĩnh. `git diff core/` rỗng, engine + brand-lint PASS | **KHÔNG tự làm được vì máy dev là macOS:** (1) push để CI dựng lại (chờ chủ dự án cho phép push); (2) mắt người trên Windows xác nhận app mở được + gõ ra dấu (`T1`) + chuông (`B0`); (3) **draft `v0.3.0` đang ôm bộ cài hỏng này — đừng publish tới khi có bản mới** | đã chốt *(chờ push + mắt người)* |
| 2026-07-18 | Ký số bản Windows — chọn SignPath Foundation, publisher hiện tên tổ chức khác | Bộ cài `.exe` chưa ký → SmartScreen chặn "Unknown publisher" trên máy MỌI người dùng khác (Unblock-File chỉ gỡ được trên máy chủ dự án). Ký số là đường DUY NHẤT hợp lệ. Khảo giá thật (web, 2026-07-18): Azure Trusted Signing $9.99/th nhưng cá nhân CHỈ mở cho US/Canada — VN loại; EV cert ~$400–600/năm + HSM, mang đúng tên chủ dự án; **SignPath Foundation miễn phí cho OSS** (mindful-key là GPL v3 + repo công khai → đủ điều kiện) nhưng publisher SmartScreen hiện **"SignPath Foundation"**, không phải tên chủ dự án | **Chủ dự án chốt SignPath (2026-07-18).** Đánh đổi publisher-name đã nêu rõ trước khi chốt và được chấp nhận. Kế hoạch + hồ sơ đối chiếu điều kiện: `docs/WINDOWS-CODE-SIGNING.md`. **CHƯA nối CI** — SignPath cấp org id/project slug/policy slug/API token SAU khi duyệt; viết YAML bằng slug tự chế là bịa cấu hình (luật cấm). SignPath cũng đòi "đã phát hành ở đúng dạng cần ký" → bị chặn bởi chính bug khởi động ở dòng trên | **Việc của chủ dự án:** (1) nộp đơn tại signpath.org — chạy song song ngay, duyệt mất vài ngày–vài tuần; (2) sau duyệt: lưu secret `SIGNPATH_API_TOKEN` + đưa tôi 3 slug để nối `release.yml`. **Để ngỏ:** có đổi sang EV (tên mình, có phí) không | mở *(chờ nộp đơn + duyệt)* |

| 2026-07-18 | Mở rộng auto-quit sang EVKey / GoTiengViet | Quyết định "thấy bộ gõ khác thì TỰ TẮT, không hỏi" chủ dự án chỉ chốt cho **OpenKey** (2026-07-17). Báo cáo 0.2.1 mục 5.1 đề nghị bắt thêm EVKey/UniKey/GoTiengViet nhưng trong bối cảnh **hộp thoại 2 nút** — chưa ai chốt auto-quit áp cho bộ gõ ngoài OpenKey. Bundle id cũng không xác minh được tại chỗ (máy dev không cài) | Suy diễn: cùng một xung đột (2 event tap giành phím) thì cùng một luật — áp allow-list 3 bộ gõ vào đúng mạch auto-quit sẵn có (`IsConflictingInputMethodBundleID`). Id lấy từ Homebrew cask (zap path): `com.lamquangminh.evkey`, `com.trankynam.GoTiengViet`; UniKey không có bản macOS chính thức → bỏ qua, không bịa. Gỡ 1 bộ gõ khỏi lưới = xoá 1 dòng trong mảng | Chủ dự án phê chuẩn: (a) auto-quit có áp cho EVKey/GoTiengViet như OpenKey không, hay bộ gõ ngoài OpenKey nên hỏi trước; (b) một lần `defaults read` trên máy có cài thật để khớp id | mở |
| 2026-07-18 | Sàn macOS: 10.15 → 13.0 | Báo cáo 0.2.1 mục E: khai 10.15 nhưng chưa từng test máy < 14.8.3 — "hứa suông". Sửa thì phải CHỌN sàn mới, mà chọn sàn = quyết định sản phẩm (cắt người dùng máy cũ), không có luật sẵn | Chọn **13.0**: sàn thấp nhất mọi tính năng chạy đủ (SMAppService login-item cần 13+; nhánh dự phòng < 13 dựa Helper `com.tuyenmai.OpenKeyHelper` không tồn tại — dưới 13 "Bật cùng hệ thống" chết im lặng sẵn rồi). Revert = sửa 2 số trong `platforms/apple/project.yml` | Chủ dự án chốt sàn chính thức: giữ 13.0, hay hạ lại (kèm cam kết test thật máy đời đó), hay nâng 14.0 (khớp máy đã test duy nhất). Nhánh dự phòng < 13 giờ là dead code — có gỡ không? | mở |

| 2026-07-18 | Audit cross-shell theo báo cáo lỗi macOS 0.2.1 — cùng LOẠI lỗi khởi động có ở nhiều vỏ | Chủ dự án đưa báo cáo lỗi 0.2.1 (dịch ngược binary): 5 lỗi A–E. Audit 26 agent (22 phát hiện, mỗi cái bị 1 agent khác mở đúng file:dòng phản biện, **0 bị bác**) xác nhận: macOS lỗi A/B (tự tắt câm + SIGSEGV terminate-giữa-launch) + D (chuỗi xin quyền) **ĐÃ VÁ thật, chứng minh bằng code chạy**; C (notarize) chờ $99; E (SDK 26.5 vs min 10.15) chờ máy thật. Phát hiện lớn: macOS lỗi B và bug khởi động Windows (`e68f101`) là **CÙNG MỘT LOẠI** — hành động nặng lúc khởi động trước khi UI+loop dựng xong. Cây mã còn mầm bệnh đó ở Windows/iOS | **ĐÃ LÀM (2026-07-18), toàn bộ trong vỏ, `git diff core/` rỗng, brand-lint + engine PASS:** (1) `stdafx.h` — `APP_CLASS` đổi thành chuỗi riêng của fork; trước đó trùng OpenKey gốc nên `FindWindow(APP_CLASS)` bắt nhầm cửa sổ OpenKey gốc → MindfulKey **lặng lẽ không mở trên máy đang chạy OpenKey**; (2) `SystemTrayHelper.cpp` + `.h` + `AppDelegate.cpp` — `WM_MK_RIVAL_WARN` cảnh báo (deferred, có chủ) khi OpenKey gốc đang chạy giẫm phím, giọng quan sát không phán xét, KHÔNG tự tắt; (3) `SystemTrayHelper.cpp` — báo trước khi thoát khi không tạo được khay; (4) `main.cpp` — báo khi từ chối UAC thay vì thoát câm; (5) iOS `KeyboardViewController.mm` — timer block+weak self, start/stop theo viewWillAppear/Disappear, gỡ vòng-giữ-nhau. Lập `docs/LIFECYCLE-SAFETY-CONTRACT.md` (3 bất biến + grep) | **CÒN MỞ:** (a) ~~EVKey/UniKey/GoTiengViet~~ ĐÃ XONG 2026-07-18 cùng ngày (bản 0.4.1): tên tiến trình xác minh từ nguồn công khai — UniKeyNT.exe/UniKey.exe (file.net, processlibrary), EVKey.exe/32/64 (github.com/lamquangminh/EVKey), GoTiengViet.exe (trankynam.com) — quét bằng Toolhelp32 trong WM_MK_RIVAL_WARN (luồng cửa sổ, sau khởi động, không đụng luồng hook); (b) **TẤT CẢ sửa Windows + iOS trên chưa biên dịch được** (máy dev là macOS) — CI + máy thật phải xác nhận; (c) việc đáng lo nhất vẫn là **vỏ Windows chưa một ai chạy** | đã chốt *(còn EVKey/UniKey + compile-verify)* |
| 2026-07-18 | macOS lần chạy đầu: tự tắt OpenKey có thể không để lại dấu vết nào (quyền notification chưa cấp) | Audit `new-lifecycle-1`: chủ dự án đã chốt "thấy OpenKey là tự tắt, không hỏi" (im lặng cố ý). Nhưng lần chạy ĐẦU TIÊN, quyền hiển thị notification thường chưa kịp cấp → notification best-effort không nổ → OpenKey biến mất mà người dùng không thấy dấu vết. Vi phạm nhẹ bất biến #2 của hợp đồng vòng đời | **Hỏi chủ dự án (AskUserQuestion 2026-07-18). Chốt: GIỮ NGUYÊN im lặng như đã quyết** — ưu tiên mạch chạy thẳng, chấp nhận lần đầu có thể không dấu vết. Đây là "ngoại lệ có chủ đích" mà bất biến #2 cho phép (ngoại lệ phải do quyết định, không phải bỏ sót) | Không còn — ghi làm ngoại lệ có chủ trong `LIFECYCLE-SAFETY-CONTRACT.md §#2` | đã chốt |

| 2026-07-18 | Login item tự bật lần chạy đầu (seed `RunOnStartup`=1) | Review đối kháng lộ lỗi có sẵn: Cài đặt mới tắt login item qua SMAppService nhưng không ghi key `RunOnStartup`, còn khởi động + `fillData` vẫn đọc key (seed=1 từ `loadDefaultConfig`) → lựa chọn TẮT bị lật lại BẬT. Không có luật nào nói lần chạy đầu có được tự đăng ký login item không hỏi hay không | Vá tối thiểu: `setRunOnStartup` giờ tự ghi key → 2 sổ luôn khớp, hết lật ngược ý người dùng. GIỮ NGUYÊN hành vi seed=1 (lần cài đầu vẫn tự bật chạy cùng hệ thống) vì đổi nó là quyết định sản phẩm | Chủ dự án chốt: app cài lần đầu có nên TỰ bật "chạy cùng hệ thống" không hỏi không (app lấy riêng tư/tin cậy làm gốc — đáng cân nhắc hỏi 1 câu trong onboarding) | mở |
| 2026-07-18 | `Main.storyboard` bị ghi đè hàng loạt ngoài ý | Working tree có sẵn ~79 chỗ `focusRingType="none"` (dính cả 3 ô nhập liệu thật → mất vòng focus khi Tab, tụt a11y) + toàn bộ XML comment `[MINDFUL]` Epic 3 bị xoá — dấu vết kinh điển của Interface Builder tự ghi lại file khi mở bằng Xcode, KHÔNG thuộc thay đổi nào được khai báo | KHÔNG commit phần storyboard này (loại khỏi commit đợt 2026-07-18); giữ nguyên working tree cho chủ dự án tự quyết | Chủ dự án quyết: nếu không phải chủ đích thì `git checkout -- platforms/apple/Resources/Base.lproj/Main.storyboard` để trả về bản gốc; nếu là chủ đích thì phải trả lại focus ring cho 3 ô nhập liệu (F2s-m3-A6w, Vlj-ox-8lT, e48-Ve-MTZ) + ghi lại comment 436pt | mở |

| 2026-07-18 | `RELEASE_PAT` trả 403 lúc phát hành v0.4.0 — release đứng tên bot | Annotation công khai của run v0.4.0 (commit `8a23991`): "RELEASE_PAT có mặt nhưng đăng không được... HTTP 403: Resource not accessible by personal access token". Hôm 2026-07-17 (v0.3.0) PAT này còn đăng được — hỏng trong vòng 1 ngày, khả năng PAT fine-grained thiếu quyền `Contents: Read and write` cho repo này hoặc bị đổi/thu hồi. Fallback github.token đã cứu: **release vẫn ra công khai đủ 6 asset**, chỉ đứng tên `github-actions` thay vì chủ tài khoản | Không chặn gì (thiết kế fallback từ v0.2.1 đã lường đúng cảnh này). Janitor draft nay chạy bằng `github.token` (draft-janitor.yml) — không phụ thuộc PAT | Chủ dự án (KHÔNG gấp): muốn release đứng tên mình thì vào GitHub Settings → Developer settings → Personal access tokens, cấp lại token có quyền `Contents: Read and write` cho repo `mindful-key`, dán vào secret `RELEASE_PAT`. Không làm thì mọi release sau vẫn ra bình thường, đứng tên bot | mở |

| 2026-07-18 | Audit "cài lại" (đúng kịch bản người dùng dính 0.3.0) — cuộc khám AI hết hạn mức chi tiêu GIỮA CHỪNG (29/41 agent phản biện lỗi "monthly spend limit", kể cả agent tổng hợp cuối) | KHÔNG được coi là "đã kiểm chứng đầy đủ" — chỉ nhánh "checkUpdate/dialog" (D2) qua trọn vòng phản biện. Ba nhánh nặng nhất (D3 cài-lại, D4 thoát-app, D5 ruột-bộ-cài) chỉ có phát hiện THÔ, chưa qua agent thứ 2 đọc lại file để bác/xác nhận. Tôi tự tay đọc mã nguồn thật (Read/Grep trực tiếp, không gọi thêm agent) để verify các phát hiện P0/P1 trước khi kết luận, thay vì lặp lại vòng workflow (tránh gãy lần nữa vì cùng giới hạn) | **Xác nhận thật bằng đọc code + tra tài liệu Inno Setup chính thức (không đoán):** (1) `SystemTrayHelper.cpp` WndProc **không có case nào** cho WM_CLOSE/WM_QUERYENDSESSION/WM_ENDSESSION — rơi hết vào `DefWindowProc`; với WM_CLOSE nghĩa là cửa sổ biến mất nhưng tiến trình sống tiếp, giữ nguyên hook bàn phím + khoá .exe = "zombie nửa sống nửa chết", tái hiện đúng triệu chứng 0.3.0 qua đường khác. (2) Xác minh qua tài liệu Inno 6 chính thức (jrsoftware.org): `CloseApplications` mặc định **yes** — bộ cài CÓ cố đóng app qua Restart Manager, nhưng vì (1) nên lời xin đóng rơi vào khoảng không, Inno báo "không tự đóng được ứng dụng". (3) `AppDelegate.cpp:92` `SendMessage` xuyên tiến trình **không timeout** khi phát hiện bản đang chạy — nếu bản đó treo, tiến trình MỚI chờ vĩnh viễn, treo vô hình y hệt sự cố cũ qua nguyên nhân khác. (4) `.iss` không có `AppMutex`/`SetupMutex` — gỡ cài không có cách nào tự đóng app đang chạy; hai bản Setup có thể chạy chồng | **ĐÃ VÁ (2026-07-18, cùng ngày, chuẩn bị 0.4.2):** `SystemTrayHelper.cpp` thêm 3 case xử sạch (gọi lại đúng `onOpenKeyExit` — gỡ hook, xoá khay, `PostQuitMessage`, không tạo đường tắt thứ 2); `AppDelegate.cpp` đổi `SendMessage`→`SendMessageTimeoutW` trần 3s + tạo `CreateMutexW(..., L"MindfulKeyboardAppMutex")` sống suốt vòng đời tiến trình; `.iss` thêm `AppMutex=MindfulKeyboardAppMutex` (khớp tên) + `CloseApplications=yes` (viết tường minh) + `SetupMutex=MindfulKeyboardSetupMutex` + kèm `LICENSE.txt` cạnh binary (nghĩa vụ GPL, chính file tự nói). `core/` sạch, `make test`/`brand-lint`/`make build` xanh. **CHƯA build-verify Windows** — máy dev macOS | **THẬT LÒNG với chủ dự án — 3 việc KHÔNG tự làm được / KHÔNG sửa trong lượt này:** (a) **máy ĐANG kẹt 0.3.0 hiện tại KHÔNG tự thoát được dù đã có bản vá** — code cũ trên máy đó không biết cơ chế mới; cần MỘT lần thủ công (Task Manager → End Task `MindfulKey.exe`) trước khi cài 0.4.2, sau đó mọi lần nâng cấp về sau mới tự động; (b) D2-B2 (registry dùng chung đường OpenKey có thể ẩn cửa sổ chào lần đầu khi nâng cấp), D5-06 (cài lại vào thư mục khác để lại mục tự khởi động trỏ vào bản mồ côi), D4-04 (thoát app không lưu nốt mẫu cảm xúc đang gom dở — mất dữ liệu, KHÁC loại lỗi treo/khoá), D4-08 (worker cảm xúc có thể đua với dọn dẹp CRT lúc thoát — có thể crash lúc thoát, KHÔNG PHẢI treo) — bốn việc này CÓ THẬT nhưng khác loại lỗi (không gây treo/khoá file) hoặc mức thấp hơn, cố ý KHÔNG gộp vào lượt vá này (nguyên tắc mổ ruột thừa đừng sửa mũi); (c) build-verify Windows thật vẫn chờ CI + tay chủ dự án trên máy Windows thật, kể cả sau lượt vá này | đã chốt *(còn nợ (a)(b) ở trên, CHƯA chạy Windows thật)* |

## Ghi chú

- File này bổ cho `TEST_MATRIX.md`: **ma trận** phơi *cái gì chưa được chứng minh*,
  **friction-log** phơi *chỗ nào AI phải đoán* — hai góc mù khác nhau.
- Khi một dòng chuyển sang `đã chốt`, ghi rõ luật/quyết định nằm ở đâu (vd: "đã cập
  nhật `docs/AGENT-BRIEF.md §3.2`" hoặc "changelog CLAUDE.md ngày …").
